# プロジェクト名: [プロジェクト名を記入]

## 概要
[解析の目的・背景を1〜2文で記載]

## データ
- **生データ**: `data/raw/` — [データの種類、取得日、サンプル数]
- **前処理済みデータ**: `data/processed/` — [処理内容]
- **外部データベース**: [GenBankアクセッション番号、ゲノムバージョン等]

## ディレクトリ構成
```
project/
├── data/
│   ├── raw/           # 生データ（読み取り専用）
│   └── processed/     # 前処理済みデータ
├── scripts/           # 解析スクリプト
├── results/           # 解析結果
├── logs/              # SLURMジョブログ
├── envs/              # conda環境定義
│   └── environment.yml
└── README.md          # このファイル
```

## 環境構築
```bash
conda env create -f envs/environment.yml
conda activate [環境名]
```

## 解析の実行手順
1. [ステップ1の説明]: `sbatch scripts/step1.sh`
2. [ステップ2の説明]: `sbatch scripts/step2.sh`
3. [ステップ3の説明]: `sbatch scripts/step3.sh`

## 主要な設定・パラメータ
| パラメータ | 値 | 根拠 |
|-----------|-----|------|
| [パラメータ名] | [値] | [選択理由・参考文献] |

## 担当者・連絡先
- **実施者**: [名前] ([メール])
- **PI**: [名前]
- **開始日**: [YYYY-MM-DD]
- **最終更新**: [YYYY-MM-DD]

## 注意事項
- `data/raw/` は読み取り専用にする（`chmod -R a-w data/raw/`）
- スクラッチ領域のデータは[N日]でパージされるため、結果は定期的にバックアップする
- 個人アカウントのホームディレクトリにデータを置かない（共有アカウントまたはプロジェクト領域を使用する）
