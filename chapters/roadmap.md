# AIエージェントと学ぶ バイオインフォマティクスプログラミングの作法

> 配列解析から機械学習まで、環境構築・テスト・設計・公開のベストプラクティス
>
> **本ロードマップのスコープ：** 開発の思想・環境構築・ツールチェーン・ベストプラクティス
> Python文法やバイオインフォマティクスの理論（アラインメントアルゴリズム等）は書籍で別途学習する前提
>
> **対応ツール：** **Claude Code CLI**（Anthropic）および **Codex CLI**（OpenAI）
> 概念・ワークフローは両ツールで共通。ツール固有の操作は対照表で示す。
> 他のAIコーディングエージェント（Cursor, Windsurf等）にも考え方の多くは適用できる
>
> **演習問題：** 各章末尾に演習問題を設ける予定。章の内容をエージェントとの協働で実践する課題を中心に構成する

---

## 目次と概要

- [はじめに](hajimeni.md)

### Phase I: AIと始めるプログラミングの基礎

- [0. AIエージェントにコードを書かせる](#0-aiエージェントにコードを書かせる2026年のベストプラクティス)（[原稿](./00_ai_agent.md)）
  セットアップ、承認モード、Plan→Code→Reviewワークフロー、設定ファイル、レビュー手法、コンテキスト管理
- [1. 設計原則 — 良いコードとは何か](#1-設計原則--良いコードとは何か)（[原稿](./01_design.md)）
  KISS・DRY・YAGNI・UNIX哲学、ソフトウェア開発手法の用語
- [2. ターミナルとシェルの基本操作](#2-ターミナルとシェルの基本操作)（[原稿](./02_terminal.md)）
  AIエージェントの動作理解に必要なシェル知識、ファイルシステム、環境変数、テキスト処理コマンド
- [3. コーディングに必要な計算機科学](#3-コーディングに必要な計算機科学)（[原稿](./03_cs_basics.md)）
  データ構造と計算量、浮動小数点の精度、乱数の再現性、プログラミング言語の基礎概念
- [4. データフォーマットの選び方](#4-データフォーマットの選び方)（[原稿](./04_data_formats.md)）
  汎用・バイオ専用・ML/数値計算フォーマットの選択基準とファイル操作
- [5. ソフトウェアの構成要素 — importからpipまで](#5-ソフトウェアの構成要素--importからpipまで)（[原稿](./05_software_components.md)）
  プログラムの実行・リンクの仕組み、共有ライブラリ、Pythonモジュールシステム、依存関係、API
- [6. Python環境の構築 — pyenv・venv・conda・uv](#6-python環境の構築--pyenvvenvcondauv)（[原稿](./06_dev_environment.md)）
  Python環境管理（pyenv, venv, conda, uv）、エディタ、開発ツール

### Phase II: 信頼できるコードを育てる技術

- [7. Git入門 — コードのバージョン管理](#7-git入門--コードのバージョン管理)（[原稿](./07_git.md)）
  Gitの基礎操作、GitHub活用、コードの公開と引用可能性、コミットの作法、大規模データの共有、セマンティックバージョニング
- [8. コードの正しさを守るテスト技法](#8-コードの正しさを守るテスト技法)（[原稿](./08_testing.md)）
  テスト駆動開発、pytest、コード品質ツール（ruff, mypy）
- [9. デバッグの技術 — tracebackから最小再現例まで](#9-デバッグの技術--tracebackから最小再現例まで)（[原稿](./09_debug.md)）
  tracebackの読み方、最小再現例、デバッガ活用
- [10. ソフトウェア成果物の設計 — スクリプトからパッケージまで](#10-ソフトウェア成果物の設計--スクリプトからパッケージまで)（[原稿](./10_deliverables.md)）
  成果物の形式選択（スクリプト・ライブラリ・パイプライン等）とプロジェクト構造設計
- [11. コマンドラインツールの設計と実装](#11-コマンドラインツールの設計と実装)（[原稿](./11_cli.md)）
  argparse/click/typer、プログレス表示、ロギング設計

### Phase III: データを扱うコードを書く

- [12. データ処理の実践 — NumPy・pandas・polars](#12-データ処理の実践--numpypandaspolars)（[原稿](./12_data_processing.md)）
  NumPy・pandas・SciPy・polars、Biopython等のバイオ専用ライブラリ
- [13. 可視化の実践 — matplotlib・seaborn・plotly](#13-可視化の実践--matplotlibseabornplotly)（[原稿](./13_visualization.md)）
  matplotlib・seaborn・plotly、可視化の原則、バイオ専門ツール
- [14. 解析パイプラインの自動化 — Snakemake・Nextflow](#14-解析パイプラインの自動化--snakemakenextflow)（[原稿](./14_workflow.md)）
  Snakemake・Nextflow・makeによる多段階パイプラインの管理

### Phase IV: ソフトウェア再現性と大規模計算

- [15. コンテナによるソフトウェア環境の再現 — Docker・Apptainer](#15-コンテナによるソフトウェア環境の再現--dockerapptainer)（[原稿](./15_container.md)）
  Docker・Apptainerによる環境の完全固定と研究再現性、実験管理（§15-6）
- [16. スパコン・クラスタでの大規模計算](#16-スパコンクラスタでの大規模計算)（[原稿](./16_hpc.md)）
  SLURM、ジョブスクリプト、GPUクラスタでのデータ管理
- [17. コードのパフォーマンス改善 — プロファイリングと高速化](#17-コードのパフォーマンス改善--プロファイリングと高速化)（[原稿](./17_performance.md)）
  プロファイリング、高速化テクニック、メモリ最適化

### Phase V: 共有のためのコード整備

- [18. コードのドキュメント化](#18-コードのドキュメント化)（[原稿](./18_documentation.md)）
  Markdown、文芸的プログラミング、README、docstring、Sphinxによるドキュメント整備
- [19. 公共データベースとAPI — データ取得の実践](#19-公共データベースとapi--データ取得の実践)（[原稿](./19_database_api.md)）
  DB一般知識（SQL/RDF/SPARQL）、バイオ系DB、APIによるデータ取得、ローカル管理
- [20. コードとデータのセキュリティ・倫理](#20-コードとデータのセキュリティ倫理)（[原稿](./20_security_ethics.md)）
  APIキー管理、データ倫理、患者データの取り扱い
- [21. 共同開発の実践 — レビュー・質問・OSS参加](#21-共同開発の実践--レビュー質問oss参加)（[原稿](./21_collaboration.md)）
  質問の技術、コードレビューとOSS参加、共同研究のコミュニケーション

### 付録

- [付録A. AIコーディングエージェントとの効果的な学習パターン](#付録a-aiコーディングエージェントとの効果的な学習パターン)（[原稿](./appendix_a_learning_patterns.md)）
  学習場面ごとのAIエージェント活用パターン一覧
- [付録B. Claude Code CLI / Codex CLI クイックリファレンス対照表](#付録b-claude-code-cli--codex-cli-クイックリファレンス対照表)（[原稿](./appendix_b_cli_reference.md)）
  Claude Code CLI / Codex CLI の操作対照表
- [付録C. 論文投稿前チェックリスト](#付録c-論文投稿前チェックリスト)（[原稿](./appendix_c_checklist.md)）
  コード・データ・再現性・バックアップの投稿前総点検

---

## 0. AIエージェントにコードを書かせる（2026年のベストプラクティス）

> 📖 原稿: [00_ai_agent.md](./00_ai_agent.md)

### 0-1. セットアップと基本操作

#### なぜCLIを使うのか — ブラウザチャットとの違い

#### インストール

| | Claude Code CLI | Codex CLI |
|--|----------------|-----------|
| インストール | `npm install -g @anthropic-ai/claude-code` | `npm install -g @openai/codex` |
| 前提 | Node.js 18+ | Node.js 18+（またはbrew/バイナリ） |
| 認証 | Claude Pro/Max契約 or Anthropic APIキー | ChatGPT Plus/Pro契約 or OpenAI APIキー |
| 起動 | `claude` | `codex` |

#### 承認モード — 3段階の安全レベル

両ツールとも「AIにどこまで自由にやらせるか」を3段階で制御する。概念は同じ:

| 安全レベル | 概念 | Claude Code | Codex CLI |
|-----------|------|-------------|-----------|
| 読み取り専用 | コード変更を一切行わず、調査と計画のみ | **Plan Mode** (`Shift+Tab` or `/plan`) | **Read-Only** (`-s read-only`) |
| 承認あり（デフォルト） | 変更のたびに人間が許可 | **Normal Mode** | **Auto**（デフォルト） |
| 全自動 | 編集・実行を自動承認 | **Auto-Accept Mode** (`Shift+Tab`) | `--full-auto` |

**初心者は「承認あり」を基本にすること。** 全自動モードは信頼できるタスクでのみ使う。

#### はじめてのVibe coding体験 — 遺伝子予測を題材に

- 題材: *E. coli* K-12 MG1655 ゲノム断片（20,000 bp）を使った遺伝子予測
- 入力データ: `scripts/ch00/data/ecoli_k12_fragment.fasta`（GenBank U00096.3、1–20,000 bp）
- Step 1: 全ORF検出（コード例・出力例あり）→ 279個の結果 → 生物学の知識で一部は判断できるが、コード設計の判断力が足りないと気づく
- Step 2: HMM + Viterbiで絞り込み（コード抜粋・出力例・ORFマップ図あり）→ 19個に → HMMパラメータの妥当性は判断できないと気づく
- 可視化: `figures/ch00_orf_comparison.png`（全ORF vs HMM予測 vs GenBankアノテーションの3パネル比較図）
- 体験のまとめ: コードを1行も書かずに完成、判断力の必要性、結果→改善のサイクル、§0-2ワークフローへの伏線

#### 基本コマンド

| 操作 | Claude Code | Codex CLI |
|-----|-------------|-----------|
| プロジェクト初期化 | `/init` → CLAUDE.md生成 | AGENTS.mdを手動作成（または `/` コマンドで雛形） |
| 計画モードに切替 | `/plan` or `Shift+Tab`×2 | `-s read-only` / `$create-plan` skill |
| モデル切り替え | `/model` | `/model` |
| セッション再開 | `/resume` | `codex resume` / `codex resume --last` |
| コンテキスト圧縮 | `/compact` | （自動管理） |
| 直前の変更を取り消し | `Esc Esc` (`/rewind`) | git revertで対応 |
| 計画をエディタで編集 | `Ctrl+G` | — |
| 非対話実行 | `claude -p "..."` | `codex exec "..."` |
| ヘルプ | `/help` | `/help` |

### 0-2. Plan → Execute → Review ワークフロー

AIコーディングエージェントを使う上で最も重要な原則は **「いきなり実装させない」** ことである。
Claude Code / Codex CLI ともに推奨される4フェーズワークフロー:

```
┌─────────────────────────────────────────────────────────┐
│  Phase 1: Explore（探索）                                │
│    読み取り専用モードで関連コードを読ませる                │
│    「src/auth/ を読んでセッション管理を理解して」           │
│    Claude Code: Plan Mode / Codex: -s read-only          │
│                                                          │
│  Phase 2: Plan（計画）                                   │
│    読み取り専用モードのまま実装計画を立てさせる             │
│    不明点はエージェントから質問させる                      │
│    Claude Code: Ctrl+G で計画を直接編集                   │
│                                                          │
│  Phase 3: Execute（実装）                                │
│    承認あり or 全自動モードに切り替え                      │
│    計画に沿って実装 → テスト実行 → 修正                   │
│                                                          │
│  Phase 4: Commit（確定）                                 │
│    git diff で変更を確認                                  │
│    テストを実行して全て通ることを確認                      │
│    意味のあるコミットメッセージで記録                      │
└─────────────────────────────────────────────────────────┘
```

**なぜPlan Modeから始めるのか：**
- AIに自由に書かせると「自信満々だが的外れなコード」が生成されることがある
- 計画フェーズは実装フェーズより圧倒的にトークンコストが低い
- 方向修正は計画段階のほうが遥かに安い（実装後のやり直しは高コスト）
- 人間がアーキテクチャの判断をし、AIが実装の詳細を担う、という役割分担が自然に生まれる

**初心者が特に意識すべきこと：**
- 読み取り専用モード中にエージェントが質問してきたら丁寧に答える。この質問への回答が実装品質を大きく左右する
- 計画に納得がいかなければ修正する（Claude Code: `Ctrl+G` で直接編集、Codex: 会話で修正指示）。受け身にならない
- 実装が計画から逸脱したら、一度読み取り専用モードに戻って計画を修正する

#### 「インタビューしてもらう」パターン — 要件が曖昧なときの最強テクニック

自分自身が「何を作りたいか」をうまく言語化できないとき、**エージェントに要件をインタビューさせる**のが非常に有効。
作りたいものの最小限の説明だけ渡して、エージェントに質問攻めにしてもらう。

```
> /plan
> RNA-seqの発現量データからDEG（差次的発現遺伝子）を抽出するCLIツールを作りたい。
> 実装を始める前に、仕様を固めるためにインタビューして。
> 必要な要件を質問して、全体像を理解してから仕様書を作って。
```

エージェントは以下のような質問を投げてくる:
- 入力データの形式は？（カウントマトリクス？ TPM？ どのツールの出力？）
- 比較のデザインは？（2群比較？ 多群？ ペアドサンプル？）
- 統計手法の選択は？（DESeq2ラッパー？ 独自実装？）
- 出力形式は？（TSV？ ボルケーノプロット？ エンリッチメント解析まで？）
- フィルタリング条件は？（FDR閾値、fold change閾値はユーザー指定？）
- 再現性の要件は？（乱数シード固定？ バージョン記録？）

**このパターンが有効な場面：**
- 新しいツールを一から設計するとき
- ドメイン知識はあるがソフトウェア設計の経験が薄いとき（実験系研究者に多い）
- 要件が頭の中にあるが整理されていないとき

**ポイント：**
- 最初のプロンプトは粗くてよい。エージェントの質問に答えていくことで仕様が固まる
- インタビューの結果を仕様書（spec）としてまとめさせる
- 仕様書を確認・修正してから、**新しいセッション**で仕様書を渡して実装を開始する
  - 新しいセッションにする理由: インタビュー過程の大量のやり取りでコンテキストが消費されるため
  - 仕様書がインタビューの「成果物」として、セッション間の橋渡しになる
- 仕様書は `docs/spec.md` としてリポジトリにコミットしておくと、後から設計意図を追える

### 0-3. プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）

プロジェクトのルートに設定ファイルを置くことで、エージェントの振る舞いをカスタマイズできる。

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| ファイル名 | `CLAUDE.md` | `AGENTS.md` |
| 自動生成 | `/init` で生成 | 手動作成（テンプレートを用意すると楽） |
| スコープ | プロジェクト / ユーザー / `~/.claude/` | プロジェクト / ユーザー / `~/.codex/` |

**書くべき内容は同じ。** 以下はバイオインフォプロジェクトの例:

```markdown
# CLAUDE.md / AGENTS.md（共通の内容）

## プロジェクト概要
- RNA-seqパイプラインのPythonラッパーツール
- Python 3.11+, conda環境を使用

## コマンド
- `pytest tests/` — テスト実行
- `ruff check src/` — リント
- `ruff format src/` — フォーマット

## コーディング規約
- 型ヒント必須
- docstringはGoogle style
- テストは `tests/` 配下に `test_*.py` で配置
- ログは `logging` モジュールを使う（`print()` 禁止）

## バイオインフォ固有の注意
- ゲノム座標はBED形式（0-based, half-open）に統一
- BAMファイルの操作は `pysam` を使用
- FASTAのパースは `Bio.SeqIO` を使用（自作パーサは禁止）
```

**設定ファイルのコツ：**
- エージェントが自動で読み取れる情報（コードから推測可能なこと）は書かない — 簡潔に保つ
- 「やってほしいこと」より「やってはいけないこと」の方が効果的
- バイオインフォ特有の規約（座標系、使用ライブラリ）は明示する
- ビルド・テスト・リントのコマンドは必ず書く（エージェントが自分でテスト実行して検証できる）

#### エージェントの拡張機能 — MCP・フック・カスタムコマンド

設定ファイルの他に、エージェントの能力そのものを拡張する4つの仕組みのショーケース:
- **MCP**（Model Context Protocol）— 外部ツール・DB連携（→§5-5で詳細）
- **フック**（Hooks）— ツール実行前後の自動チェック（→§8-3で詳細）
- **カスタムコマンド**（Agent Skills）— 定型指示のテンプレート化（→§11-1で詳細）
- **設定ファイルの階層構造** — ディレクトリ単位のルール設定（→§10-3で詳細）

> 🧬 コラム: バイオインフォマティクス向けMCPサーバーとAIエージェント（BioMCP, PubMed MCP等のMCPサーバー + Biomni（Stanford大）のAIエージェント。→§19で詳細）

### 0-4. エージェントにレビューさせる

AIコーディングエージェントは**実装だけでなくレビューにも使える**。
人間のレビューを補完する形で、以下のパターンが有効:

#### 実装後のセルフレビュー
```
# 読み取り専用モードで実装結果をレビューさせる
> 今回の変更をレビューして。以下の観点で問題がないか確認して:
> - エッジケースの見落とし
> - エラーハンドリングの不足
> - パフォーマンス上の懸念
> - 既存テストとの整合性
```

#### 「別人格」レビュー
```
# 新しいセッションを開いて、実装コードを批判的にレビューさせる
> git diffでの変更内容を、シニアエンジニアとしてレビューして。
> バグの可能性、設計上の問題点、テスト不足を厳しく指摘して。
```

#### テスト生成によるレビュー
```
# 実装に対するテストを書かせることで、暗黙の仕様を炙り出す
> src/analysis.py に対するテストを書いて。
> 特にエッジケース（空の入力、不正なFASTAファイル、巨大ファイル）を重視して。
```

**レビューのベストプラクティス：**
- 実装とレビューは**別のセッション**で行うと、コンテキストの偏りが減る
- エージェントのレビュー結果を鵜呑みにしない — 科学的妥当性は人間が判断する
- レビューで指摘された問題は、修正後にテストを追加して再発を防ぐ

### 0-5. サブエージェントとタスク委譲

両ツールとも**サブエージェント**を生成して並列にタスクを実行できる。

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| 自動サブエージェント | Explore Subagent（Plan Mode中にHaikuで自動起動） | — |
| 明示的な委譲 | 「use subagents」と指示 | 「use subagents」と指示 |
| 並列分離 | ワークツリー分離（`isolation: worktree`） | サブエージェントごとに独立実行 |

**初心者が知っておくべきこと：**
- サブエージェントはメインのコンテキストを消費しないため、大規模な調査に向く
- 明示的に「use subagents」「サブエージェントで並列に」と指示すると委譲されやすい
- 各サブエージェントもトークンを消費するため、コスト意識を持つ

### 0-6. コンテキスト管理

AIコーディングエージェントの**最大の制約はコンテキストウィンドウ**（現状200K〜1Mトークン）。
長いセッションでは、初期の指示を「忘れる」ことがある。

**対策：**
- **こまめにコミット** — 1タスク完了ごとにコミット。セッションが壊れても戻れる
- **コンテキスト圧縮** — Claude Code: `/compact`、Codex: 自動管理
- **セッションの切り分け** — 大きなタスクは複数セッションに分割
- **プロジェクト設定ファイル** — セッションをまたいで保持すべき情報は CLAUDE.md / AGENTS.md に書く
- **セッション再開** — Claude Code: `/resume`、Codex: `codex resume`

### 0-7. モデル選択・推論の深さ・コスト意識

AIコーディングエージェントの性能を調整するには **「どのモデルを使うか」** と **「どれだけ深く考えさせるか」** の2つの軸がある。

#### モデルの階層

| 役割 | Claude Code | Codex CLI |
|-----|-------------|-----------|
| 最高精度（複雑な設計判断、レビュー） | **Opus** | **GPT-5.4** |
| バランス（日常の実装、デフォルト） | **Sonnet** | **GPT-5.3-Codex** |
| 高速・低コスト（探索、簡単な修正） | **Haiku** | **GPT-5.3-Codex-Spark** |
| 切り替え方法 | `/model` | `/model` |

#### 推論の深さ（Reasoning Depth）— 2つ目の調整軸

モデル選択とは**独立して**、エージェントにどれだけ「考えさせるか」を調整できる。
推論を深くするほど精度が上がるが、レイテンシとコストが増える。

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| 概念 | **Extended Thinking** | **Reasoning Effort** |
| 段階 | ON / OFF（Opus 4.6ではadaptive thinkingが自動調節） | None / Low / Medium / High / Extra High |
| 切り替え方法 | `Alt+T`（macOS: `Option+T`）でトグル | `/model` → Reasoning effort、または`-c model_reasoning_effort="high"` |
| 計画時の設定 | Plan Mode + Extended Thinking の組み合わせ | `plan_mode_reasoning_effort` で計画時だけ別の深さを設定可能 |
| 設定の永続化 | — | `~/.codex/config.toml` に `model_reasoning_effort = "high"` |

#### 使い分けの指針

```
簡単な修正（タイポ修正、1行変更）
  → 軽量モデル + 低い推論深度
    Claude Code: Sonnet（adaptive thinkingに任せる）
    Codex: GPT-5.3-Codex, Reasoning = Low

日常的な実装（関数追加、テスト生成）
  → デフォルトモデル + デフォルト推論深度
    Claude Code: Sonnet
    Codex: GPT-5.3-Codex, Reasoning = Medium（デフォルト）

複雑な設計判断（アーキテクチャ、リファクタリング計画）
  → 高精度モデル + 高い推論深度
    Claude Code: Opus + Extended Thinking ON
    Codex: GPT-5.4, Reasoning = High / Extra High
```

**コスト意識：**
- サブスクリプション契約（Claude Max, ChatGPT Pro等）はトークン上限なし/大
- API利用は従量課金 — 高精度モデル×高推論深度は5〜10倍のコスト
- 「全てに最高性能を使う」のではなく、タスクに応じて使い分ける

### 0-8. AIに頼るべきこと・自分で判断すべきこと

| AIが得意なこと | 人間が判断すべきこと |
|--------------|-------------------|
| 定型コードの生成 | アーキテクチャ・設計方針 |
| APIの使い方・ライブラリの呼び出し | データ構造・フォーマットの選択 |
| エラーメッセージの解読 | 科学的妥当性の検証 |
| テストコードの生成 | テストすべきケースの選定 |
| リファクタリング | 分割の粒度と命名の判断 |
| ドキュメントの下書き | 内容の正確性の確認 |
| SLURMスクリプトの雛形 | リソース要求量の見積もり |

**「動くコード」と「良いコード」の違い：**
- AIが生成したコードは「動く」ことが多いが、それが「良い」とは限らない
- 不要な複雑さ、非効率なアルゴリズム、ハードコーディングされた設定を見抜く目が必要
- レビュー（§0-4）とテスト（§8）で品質を担保する

### 0-9. 車輪の再発明を防ぐ

- エージェントに「この処理を実装して」と頼む前に、既存ツール/ライブラリがないか確認する
- **判断基準**: 既存ツールが成熟・テスト済みなら呼ぶ。学習目的やカスタマイズが必要なら実装する
- プロジェクト設定ファイルに「FASTAパーサは `Bio.SeqIO` を使え、自作禁止」のように明記する
- 「この処理を実装する前に、既存のPythonライブラリやCLIツールで解決できないか確認して」と聞く習慣をつける

### 0-10. セキュリティと権限管理

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| 権限ホワイトリスト | `/permissions`（例: `Bash(pytest *)`, `Edit(src/**)`） | サンドボックス設定（`sandbox_mode`） |
| 全自動の危険な設定 | Auto-Accept Mode | `--full-auto` / `--dangerously-bypass-approvals-and-sandbox` |

- **全自動モードは信頼できるタスクでのみ使う** — 初心者はデフォルト（承認あり）を基本に
- APIキーやパスワードがコードに紛れ込んでいないかコミット前に確認
- `.gitignore` と `.env` の設定をプロジェクト設定ファイルに明記して、秘密情報を扱わせない

---

## 1. 設計原則 — 良いコードとは何か

> 📖 原稿: [01_design.md](./01_design.md)

### 1-1. コードの設計原則
- **KISS原則**（Keep It Simple, Stupid）— 不必要な複雑さを避ける
- **DRY原則**（Don't Repeat Yourself）— 重複コードの排除と関数化
- **YAGNI原則**（You Aren't Gonna Need It）— 必要になるまで作らない
- **関心の分離** — 機能ごとにモジュールを分ける
- **単一責任原則** — 1つの関数は1つのことだけ行う
- **最小驚き原則** — 予測可能な振る舞いをする

### 1-2. UNIX哲学
- 「一つのことをうまくやるプログラムを書け」
- テキストストリームをユニバーサルインターフェースとする
- パイプ（`|`）とリダイレクション（`>`, `>>`, `<`, `2>&1`）
- stdin / stdout / stderr の使い分け
- 小さなツールの組み合わせで複雑な処理を実現
- 終了ステータス（exit code: 0 = 成功、非0 = 失敗）

### 1-3. ソフトウェア開発手法の用語 — AIエージェントと会話するための語彙

AIコーディングエージェントに計画を立てさせると「スプリントで分割」「MVPから始める」「リファクタリングのイテレーション」等の用語が当然のように出てくる。
これらはソフトウェア業界で広く使われている開発手法の用語であり、意味を知らないとAIの提案を正しく評価できない。

#### 開発プロセスモデル

**ウォーターフォール** — 要件定義→設計→実装→テスト→リリースを順番に進める古典的モデル。前のフェーズに戻りにくい。
大規模な受託開発で使われてきたが、研究開発のように要件が変わりやすいプロジェクトには不向き。

**アジャイル開発** — 小さな単位で「動くもの」を繰り返し作りながら改善する考え方。ウォーターフォールの対極。
以下のスクラムやカンバンはアジャイルの具体的な実践手法。
研究のプログラミングは本質的にアジャイル的（要件が進行中に変わる）。

**スクラム** — アジャイルの代表的なフレームワーク。以下の用語が頻出する:
- **スプリント** — 1〜2週間の固定期間の開発サイクル。「この機能をこのスプリントで完成させる」
- **バックログ** — やるべきタスクの優先順位つきリスト。GitHub Issuesで管理できる
- **スタンドアップ** — 毎日の短い進捗共有（1人開発では不要だが、用語として知っておく）
- **レトロスペクティブ** — スプリント終了後の振り返り。何がうまくいき、何を改善すべきか

**カンバン** — タスクを「To Do → In Progress → Done」のボード上で視覚管理する手法。
GitHub ProjectsやNotionのボードビューがこれ。進行中のタスク数に上限（WIP制限）を設けるのが特徴。

#### よく出てくる開発プラクティスの用語

| 用語 | 意味 | AIエージェントとの関連 |
|-----|------|-------------------|
| **MVP**（Minimum Viable Product） | 最小限の動くプロダクト。まず核心機能だけ作り、後から拡張する | エージェントが「まずMVPを作りましょう」と提案してきたら、全機能を一度に実装しない戦略のこと |
| **イテレーション** | 「計画→実装→評価」を繰り返すこと | Plan→Execute→Reviewサイクル（§0-2）そのもの |
| **リファクタリング** | 動作を変えずにコードの内部構造を改善する | 「テストを通してからリファクタリング」が鉄則 |
| **技術的負債**（テクニカルデット） | 急いで書いたコードが将来の開発を遅くする「借金」 | エージェントが「技術的負債になる」と警告したら、設計を見直すサイン |
| **スコープクリープ** | 開発中に要件がどんどん膨らむこと | エージェントに「あれもこれも」と追加依頼し続けると発生する |
| **ブロッカー** | 他の作業を止めてしまう障害 | 依存ライブラリのインストール失敗など。解決するまで先に進めない |
| **デプロイ** | ソフトウェアを実際に使える環境に配置すること | HPCにツールを入れて使えるようにする = デプロイ |
| **CI/CD** | 継続的インテグレーション/デリバリー | コミットするたびにテストが自動実行される仕組み（§8-4） |
| **コードレビュー** | 他の人（やAI）にコードを読んでもらい問題を見つける | §0-4 のレビューワークフロー |
| **ペアプログラミング** | 2人で1つのコードを書く手法 | AIエージェントとの対話は一種のペアプログラミング |
| **プルリクエスト**（PR） | 変更を本体に統合する前のレビュー依頼 | エージェントに `gh pr create` でPRを作らせられる |
| **マイルストーン** | 大きな目標に向けた中間の達成地点 | 「Phase 1のマイルストーン: FASTQの読み込みとQCが動く」 |

#### 一人開発で実際に使うもの

上記の全てを厳密に実践する必要はない。一人で研究ツールを開発する場合に特に有用なのは:

- **MVPの考え方** — 最初から完璧を目指さない。まず最小限動くものを作り、使いながら改善する
- **イテレーション** — Plan→Execute→Reviewの繰り返し（§0-2のワークフローそのもの）
- **バックログ**（≒ GitHub Issues） — やりたいことをIssueに書き出して優先順位をつける
- **リファクタリング** — 「動いたけど汚いコード」をテストで保護してから整理する
- **技術的負債の意識** — 「今は動くけど後で困る」コードを放置しない

エージェントが「スプリントで区切りましょう」と提案してきたら、
それは「1-2週間で達成可能な小さなゴールを設定して段階的に進めよう」という意味。
大げさに構える必要はなく、研究のマイルストーン設計と同じ感覚で理解すればよい。

---

## 2. ターミナルとシェルの基本操作

> 📖 原稿: [02_terminal.md](./02_terminal.md)

### 2-1. なぜVibe codingにシェルの知識が必要か
- AIエージェントはシェルを介してコードを実行する
- Claude Code / Codex CLIの思考過程に `grep`, `find`, `ls` 等のコマンドが表示される
- エージェントが何をしているか理解し、レビューするために必要

### 2-2. ファイルシステムとパーミッション
- ディレクトリ構造（`/home`, 相対パス / 絶対パス）
- パーミッション — 実行権限とshebang行（`#!/usr/bin/env python3`）に集中
- プロセス管理（`ps`, `kill`）— 1段落で概要のみ。ジョブ管理の詳細は[§16](#16-スパコンクラスタでの大規模計算)で
- 圧縮・展開（`gzip`, `tar`）— FASTQの `.gz` で動機付け

### 2-3. 環境変数とパス
- `PATH` 変数の意味と設定 — 「PATHが壊れるとエージェントがツールを見つけられない」という動機付け
- `export` と永続化（`.bashrc` 等）
- `.env` ファイルによる設定管理
- `which` によるコマンドの場所の確認

### 2-4. テキスト処理コマンド
- `grep`, `awk`, `sed`, `sort`, `uniq`, `cut`, `wc`, `find`, `xargs`
- 各コマンドの1行説明 + バイオインフォでの典型的な使用場面
- パイプ（`|`）による連結パターン
- 「Pythonで実装して」と言う前に「シェルのワンライナーで済む？」と聞く習慣
- エージェントの思考過程に出てくるコマンドとの対応
- 正規表現の基礎（メタ文字、キャプチャグループ、Pythonの`re`モジュール）

### 2-5. シェルスクリプティングの基礎
- `set -euo pipefail` の重要性
- 変数・条件分岐・ループ（最小限）
- コマンド置換（`$(command)`）
- 「本格的なCLIはPythonで（[§11](#11-コマンドラインツールの設計と実装)参照）」への橋渡し

> **📦 コラム：SSH・tmux・Vim — サーバ上でのVibe coding**
>
> - SSH鍵管理（`ssh-keygen`, `ssh-agent`）と `~/.ssh/config` の基礎
> - tmuxのデタッチ/アタッチ（接続断からの復帰）
> - Vimの最低限操作（`:wq`, `i`, `Esc`）
> - [§16 スパコン・クラスタでの大規模計算](#16-スパコンクラスタでの大規模計算)への前方参照

---

## 3. コーディングに必要な計算機科学

> 📖 原稿: [03_cs_basics.md](./03_cs_basics.md)

### 3-1. データ構造と計算量
- list, dict, set, tuple, deque の使い分け
- $O$ 記法($O(1)$, $O(\log n)$, $O(n)$, $O(n \log n)$, $O(n^2)$) — 読み方と大小関係
- 「なぜこのデータ構造を選ぶのか」を説明できること
  - 例: 10万遺伝子のID検索 → list の `in` は $O(n)$、set の `in` は $O(1)$
- collections の便利なデータ構造 — `Counter`（k-mer頻度、出現回数）、`defaultdict`（グループ化）
- `heapq` — 上位k件の抽出（top-k遺伝子、p値の上位N件）
- ソートの活用 — `sorted()` のキー関数、安定ソート、ゲノム座標ソートの考え方
- 二分探索 — `bisect` モジュール（ソート済みゲノム座標への高速アクセス）

> 🧬 コラム: バイオインフォマティクスで出会うアルゴリズムの計算量（BLAST, Smith-Waterman, BWA, PCA, t-SNE/UMAP, NJ等）

> 🧬 コラム: バイオインフォで出会うデータ構造とアルゴリズム
>
> - ハッシュテーブル（set/dict）→ k-mer counting, 重複除去
> - 木構造 → 系統樹、インデックス（B-tree: BAI/TBI）
> - グラフ → de Bruijn graph（アセンブリ）、遺伝子ネットワーク
> - 接尾辞配列 / FM-index → BWA等の高速マッピングの原理
> - 動的計画法 → アラインメント（Smith-Waterman）の核心
> ※ 実装はせず、「なぜこのツールがこの計算量なのか」を理解する概念的な紹介に留める

### 3-2. 数値表現と浮動小数点
- 整数型のビット幅（int8, int32, int64）とオーバーフロー
- **IEEE 754 浮動小数点** — float32 / float64 の精度の限界
  - **`0.1 + 0.2 != 0.3` 問題** — なぜ起きるか、どう対処するか
- **丸め誤差の蓄積** — `math.fsum()` の利用
- **比較の注意** — `==` ではなく `math.isclose()` / `np.allclose()` を使う
- 特殊な値 — `inf`, `NaN` の振る舞い（`NaN != NaN`）

> **🧬 コラム：バイオインフォで踏む浮動小数点の罠**
>
> - 正規化値（TPM, FPKM）の比較で `==` を使うと一致しない
> - p値の極小値（`1e-300`付近）→ アンダーフローで `0.0` になる → 対数p値で扱う
> - GPU計算（float32）で発現量行列の精度が落ちる → 正規化後の微小差が消失
> - Phredスコア: Q30 = 10^-3 の対数変換、精度に注意

### 3-3. 文字エンコーディング
- ASCII, UTF-8 の基礎
- **改行コードの罠** — LF（Unix）vs CR+LF（Windows）
  - Windows由来のFASTAファイルが壊れるパターン → `dos2unix`
- バイトオーダー（エンディアン）— バイナリフォーマットで意識が必要

### 3-4. 乱数と再現性
- 擬似乱数生成器（PRNG）と乱数シードの固定
- `np.random.default_rng(seed)` で再現性を確保
- 論文にシード値を記録する習慣

> **🧬 コラム：バイオインフォでの乱数再現性**
>
> - t-SNE / UMAP の結果が毎回変わる → シード固定で解決
> - GPU（cuDNN）の非決定的演算 → `torch.use_deterministic_algorithms(True)` 等
> - ブートストラップ法やランダムサブサンプリングにもシード記録が必須

### 3-5. 計算機アーキテクチャの基礎
- メモリ階層（キャッシュ → RAM → ストレージ）
- 行優先（C order）vs 列優先（Fortran order）— NumPy配列のメモリレイアウト
- CPU vs GPU の使い分け、VRAM の制約
- I/Oバウンド vs コンピュートバウンドの見極め
- ネットワークファイルシステム（NFS, Lustre）の特性

### 3-6. Pythonの型ヒント
- 基本型（int, str, float, bool, None）と関数シグネチャの読み方
- コレクション型（list[str], dict[str, float]等）
- Optional / Union（str | None）
- ジェネリック型（Sequence, Iterable, Callable）の概要
- §8-3のmypyへの前方参照

> ※ §3-5の「静的型付け vs 動的型付け」からの接続。プログラミング言語の基礎概念（パラダイム、コンパイラ/インタープリタ型）は§3-5末尾に統合

---

## 4. データフォーマットの選び方

> 📖 原稿: [04_data_formats.md](./04_data_formats.md)

### 4-0. データとメタデータ
- データとメタデータの定義・区別
- バイオインフォの具体例: FASTQファイル（データ）とサンプルシート（メタデータ）

### 4-1. 汎用フォーマット
- **TSV / CSV** — タブ/カンマ区切り、ヘッダ行の扱い
- **JSON** — 構造化データ、APIレスポンス
- **YAML / TOML** — 設定ファイル
- **Markdown** — ドキュメント記述

> **🧬 コラム: Excelと遺伝子名 — Oct4問題**
>
> ExcelがMARCH1, SEPT1, DEC1等の遺伝子名を日付に自動変換する問題。
> Ziemann et al. (2016) Genome Biology: ゲノム研究論文の約20%に遺伝子名エラー。
> Abeysooriya et al. (2021) PLOS Comp. Biol.: その後も改善せず。
> HGNCが2020年に27遺伝子をリネーム（MARCH1→MARCHF1, SEPT1→SEPTIN1等）。
> 対処法: CSVをExcelで開かずプログラムで処理する。
>
> **🧬 コラム: 日本語環境の罠 — Shift_JIS, BOM, NFD, マルチバイト文字**
>
> 日本語環境特有の4つの罠（Shift_JIS/CP932、UTF-8 BOM、macOSのNFDファイル名、全角スペース・全角数字等のマルチバイト文字混入）とその対処法。
> §3-3の概念を踏まえた実践編。

### 4-2. フォーマット選択の判断基準
- テキスト vs バイナリの使い分け
- 圧縮の選択（gzip, zstd）
- インデックス付きフォーマットの利点（ランダムアクセスの高速化）
- 下流ツールが受け付ける形式から逆算して選ぶ
- 機械可読なデータ形式の条件（1行1レコード、ヘッダ行が1行目、セル結合なし、型の一貫性）
- 人間向けレイアウトのデータをAIコーディングエージェントで機械可読形式に変換する実践
- tidy data（Wickham 2014）: long form vs wide form

> **🧬 コラム：バイオインフォの主要フォーマット**
>
> 自分でパーサを書く前に、各フォーマットの標準を知ること。
>
> | カテゴリ | 形式 | 注意点 |
> |---------|------|--------|
> | 配列 | FASTA, FASTQ | FASTAインデックス（`.fai`）でランダムアクセス可能 |
> | マッピング | SAM/BAM/CRAM | BAM+BAIが日常使い。FLAGフィールドはビットフラグ |
> | アノテーション | BED, GFF3/GTF | **BED=0-based**, **GFF=1-based** — 混同は致命的バグの元 |
> | 変異 | VCF/BCF | ヘッダ行（`##`）の構造を理解する |
> | MSA | FASTA, Clustal, Stockholm, PHYLIP | ツール間の変換は `Biopython AlignIO` or EMBOSS `seqret` |
> | BLAST出力 | outfmt 6/7（タブ区切り） | カラムはカスタマイズ可能。XML (outfmt 5) はプログラムパース向け |
> | シグナル | BigWig, BedGraph | ゲノムカバレッジ・ChIPシグナル等 |
> | シングルセル | AnnData (.h5ad) | `.X`（行列）, `.obs`（細胞）, `.var`（遺伝子） |
> | 系統樹 | Newick, Nexus | 括弧表記 `((A,B),C);` |
>
> 座標系の違い（0-based half-open vs 1-based closed）は最も多いバグ源の一つ。

> **🧬 コラム：遺伝子構造の基礎知識** — gene→transcript→exon/CDS/UTRの階層、選択的スプライシング、gene-level vs transcript-level counting、GENCODE vs RefSeq、ID変換

> **🧬 コラム：リファレンスゲノムの選び方** — hg19/hg38/T2T-CHM13、UCSC名 vs GRC名、座標のリフトオーバー、アノテーションソースの選択

> **🧬 コラム：GEO/SRA登録を見据えたメタデータ設計**
>
> 論文投稿時にGEO/SRAへのデータ登録が求められることは多い。メタデータの整備は解析の最初に設計しておくべきであり、後から整備すると手間が何倍にもなる。
>
> **GEO登録に必要なメタデータ項目（主なもの）:**
> - サンプル属性（生物種、組織、処理条件、レプリケート番号）
> - 実験プロトコル（ライブラリ調製法、シーケンサ機種）
> - プラットフォーム情報（Illumina NovaSeq等）
> - データ処理の概要（マッピングツール、パラメータ）
>
> **SRAに必要なファイル形式:**
> - 生リードデータ: FASTQ（ペアエンドの場合は R1/R2 を明確に区別）
> - マッピング済みデータ: BAM（必要に応じて）
>
> **実践: サンプルシートを最初に作る**
>
> 解析開始時にサンプルシート（TSV/CSV）を作成し、メタデータを機械可読な形で管理する。
> このシートがそのままGEO登録時のメタデータの土台になる。
> サンプルシートの設計については[§4-0](./04_data_formats.md)を参照。
>
> → 投稿前の最終確認は[付録C](./appendix_c_checklist.md)を参照

> **🤖 コラム: 機械学習・数値計算のデータフォーマット**
>
> バイオインフォ×MLでは、配列解析系とは異なるフォーマット群が登場する。
>
> | カテゴリ | 形式 | 用途 | 注意点 |
> |---------|------|------|--------|
> | 配列/テンソル | .npy / .npz | NumPy配列（単一/複数） | 中間データ保存の基本。§3-2 round-trip問題の解 |
> | 配列/テンソル | Zarr (.zarr) | チャンク化大規模配列 | 並列アクセス対応、クラウドストレージ親和性 |
> | 配列/テンソル | HDF5 (.h5) | 階層型大規模データ | AnnData (.h5ad) の基盤フォーマット |
> | 表形式 | Parquet (.parquet) | 列指向バイナリ | pandas/polars対応、圧縮効率が高い |
> | 表形式 | SQLite (.db) | 組み込みRDB | メタデータ管理に便利、SQL でクエリ可能 |
> | モデル保存 | Pickle (.pkl) | Pythonオブジェクト汎用 | 信頼できないソースのPickleは実行しない（セキュリティリスク） |
> | モデル保存 | .pt / .pth | PyTorchチェックポイント | モデル重み＋オプティマイザ状態。学習再開に必須 |
> | モデル保存 | SafeTensors (.safetensors) | 安全なテンソル保存 | Pickleの脆弱性を排除。Hugging Face推奨 |
> | モデル交換 | ONNX (.onnx) | フレームワーク非依存 | PyTorch→TensorRT等の変換に使用 |
> | TF固有 | TFRecord | TensorFlow学習データ | Protocol Bufferベース。大規模データセットの効率的読み込み |
> | R連携 | .RData / .rds | Rオブジェクト保存 | PythonからはCSV/Parquet経由が安全 |
>
> **判断の指針:**
> - 中間の数値データ → .npy / .npz / Zarr（§3-2 の round-trip 問題を避ける）
> - 大規模表形式 → Parquet（CSVの10倍以上高速、サイズも数分の一）
> - モデルの保存・共有 → SafeTensors or ONNX（Pickleは自分専用に限定）
> - R との連携 → Parquet が最も安全な橋渡し

---

## 5. ソフトウェアの構成要素 — importからpipまで

> 📖 原稿: [05_software_components.md](./05_software_components.md)

> **注:** 本章は論理的には§6（Python環境の構築）の前に位置する内容だが、暫定的に§5として配置している。将来の章再編時に適切な位置に移動する予定。

### 5-1. プログラムはどう動くか
- インタプリタ（Python）とコンパイル言語（C/C++）の違い
- コンパイル → リンク → 実行の流れ（図解）
- 静的リンク vs 動的リンク
- 共有ライブラリ（.so / .dylib）と`LD_LIBRARY_PATH`
- `ldd` / `otool -L` で依存確認
- samtools → libhts → libz の依存チェーン
- condaが共有ライブラリを環境内に管理する仕組み

### 5-2. Pythonのモジュールシステム
- モジュール（.pyファイル）/ パッケージ（ディレクトリ）/ ライブラリの定義
- `import` 文と `sys.path` の検索順序
- `site-packages`、`PYTHONPATH`
- `__init__.py` の役割
- よくあるエラーと対処（ModuleNotFoundError, ImportError）

### 5-3. 依存関係の問題
- 依存チェーン（Python → C → OS）
- 依存地獄（dependency hell）
- venv/conda が隔離するもの・しないもの → §6, §15 への橋渡し
- 依存関係の可視化ツール

### 5-4. APIとインターフェース
- API の概念（CLI / Python / Web の3レベル）
- API バージョン依存 → §7 セマンティックバージョニング参照
- SOLID 原則の概要（§1 の SRP を拡張）

### 5-5. MCP（Model Context Protocol）— エージェントの能力を拡張する
- pip installのアナロジーでMCPを理解する
- MCPの仕組み（エージェントと外部サービスの標準インターフェース）
- 汎用MCPサーバーの例（GitHub、Filesystem、Notion、PostgreSQL、Fetch）
- MCPサーバーの追加方法（`claude mcp add` / `codex mcp add`）
- MCPとAPIの違い（人間が書くコード vs エージェントが直接呼び出す）

---

## 6. Python環境の構築 — pyenv・venv・conda・uv

### 6-1. Pythonの環境管理
- **pyenv** — Pythonバージョンの管理
- **venv** — プロジェクトごとの仮想環境
- **miniforge / micromamba** — conda環境（バイオインフォでのデファクト）
- **uv** — 高速なPythonパッケージマネージャ（新世代）
- `requirements.txt` vs `pyproject.toml` vs `environment.yml`
- 仮想環境の activate/deactivate の仕組み
- 依存関係の競合（dependency conflict）の理解と解決

### 6-2. パッケージマネージャの概念
- パッケージマネージャが解決する問題（依存関係地獄）
- pip, conda, brew, apt の使い分け
- ロックファイル（`pip freeze`, `conda-lock`, `uv.lock`）の意味
- チャネルとレジストリ（conda-forge, bioconda, PyPI）

> **🧬 コラム：biocondaでのツールセットアップ**
>
> バイオインフォツールの多くは `bioconda` チャネルからインストールできる:
> ```bash
> conda install -c bioconda -c conda-forge samtools minimap2 fastp
> ```
> biocondaにないツールはソースビルドやpip installが必要。
> 環境は用途別に分ける（`mapping-env`, `variant-env`等）と依存関係の衝突を避けられる。

> **🧬 コラム：車輪の再発明を防ぐ — スイスアーミーナイフ的ツール一覧**
>
> エージェントにコードを書かせる前に「既にこれを解決するツールがあるのでは？」と立ち止まる。
> 以下は「一つ入れるだけで大量の作業が片付く」多機能ツール群:
>
> | ツール | 性質 | 代表的な機能 |
> |-------|------|-------------|
> | **samtools** | BAM/SAMの万能ナイフ | view, sort, index, flagstat, depth, merge, faidx |
> | **bedtools** | ゲノム区間の集合演算 | intersect, subtract, merge, closest, genomecov |
> | **bcftools** | VCFの万能ナイフ | call, filter, query, stats, merge |
> | **seqkit** | 配列操作（Go製、高速） | stats, grep, subseq, split2, fx2tab |
> | **EMBOSS** | 配列解析スイート（200+コマンド） | seqret（形式変換）, transeq（翻訳）, needle（NW）, water（SW）, getorf |
> | **BLAST+** | 相同性検索 | blastn, blastp, blastx, makeblastdb |
> | **MAFFT** | マルチプルアラインメント | `--auto` で自動パラメータ、L-INS-i で高精度 |
> | **MUSCLE** | マルチプルアラインメント | v5で大幅高速化 |
> | **Clustal Omega** | 大規模MSA | 数万配列に対応 |
> | **HMMER** | プロファイルHMM検索 | hmmsearch, hmmscan（Pfamドメイン検索） |
> | **MMseqs2** | 超高速クラスタリング・検索 | BLAST代替として大規模データに |
> | **DIAMOND** | 高速BLASTp/x代替 | メタゲノム等の大規模検索 |
> | **fastp** | リードQC＋トリミング一括 | FastQCの代替としても使える |
> | **minimap2** | 汎用アライナ | ロングリード〜ショートリードまで |
> | **deepTools** | BAM/BigWig解析・可視化 | bamCoverage, computeMatrix, plotHeatmap |
> | **IQ-TREE** | 系統樹推定 | 最尤法＋ModelFinder統合 |
> | **Salmon / kallisto** | 転写産物定量 | アラインメントフリーで高速 |
>
> condaでのインストール例: `conda install -c bioconda samtools seqkit mafft blast`
>
> **アラインメントツールの使い分け:**
> | データ種別 | 推奨ツール | 理由 |
> |---|---|---|
> | DNA-seq ショートリード | BWA-MEM2 | 連続マッピング前提 |
> | RNA-seq ショートリード | STAR / HISAT2 | スプライスジャンクション対応 |
> | ロングリード | minimap2 | 高エラー率対応 |
> | タンパク質相同性 | DIAMOND > BLAST+ | 速度で選択 |
> | マルチプルアラインメント | MAFFT（デフォルト） | --autoで自動パラメータ |

> **🤖 コラム：機械学習環境の構築**
>
> バイオインフォ×機械学習（scFoundationモデル、変異予測、タンパク質構造予測等）ではGPU環境が必要になる。
>
> **CUDA/PyTorchのインストール（conda推奨）：**
> ```bash
> conda create -n ml-env python=3.11
> conda activate ml-env
> conda install pytorch pytorch-cuda=12.1 -c pytorch -c nvidia
> # または: pip install torch --index-url https://download.pytorch.org/whl/cu121
> ```
>
> **よくあるハマりポイント：**
> - CUDAドライバ（OS側）とCUDA Toolkit（PyTorch側）のバージョン不一致
>   - `nvidia-smi` でドライバ版を確認、`torch.cuda.is_available()` で疎通確認
> - HPC上ではモジュールシステム（`module load cuda/12.1`）との組み合わせに注意
> - conda環境にcudatoolkitを入れる方法とシステムCUDAを使う方法を区別する
>
> **バイオインフォML系のよく使うライブラリ：**
> - **PyTorch** / **JAX** — 深層学習フレームワーク
> - **scikit-learn** — 古典的機械学習（分類、クラスタリング、前処理）
> - **Hugging Face Transformers** — 事前学習済みモデルの利用
> - **wandb** / **MLflow** — 実験追跡（§15参照）
> - **optuna** — ハイパーパラメータ最適化

---

## 7. Git入門 — コードのバージョン管理

### 7-1. Gitの基礎
- ステージング → コミット → プッシュの流れ
- ブランチとマージ、コンフリクトの解決
- `git log`, `git diff`, `git stash`
- `.gitignore`（大きなデータファイル、秘密情報の除外）

### 7-2. GitHubの活用
- リモートリポジトリ、Pull Request、Issue管理
- GitHub Actions（CI/CDの入口）
- README.md の書き方
- ライセンスの選択（MIT, Apache 2.0, GPL）
- GitHub CLI（`gh`コマンド）

#### コードの公開と引用可能性
- **GitHub Releases** によるバージョン固定 — タグを切ってリリースを作成する
- **Zenodo連携** — GitHub webhookでリリース作成時にDOIを自動発行
  - Zenodoの設定手順（GitHubアカウント連携 → リポジトリの有効化 → リリース作成でDOI発行）
- **CITATION.cff** の作成 — GitHubが「Cite this repository」ボタンを自動生成する
- **ライセンス選択の判断基準**
  - MIT: 最も緩い。商用利用可。迷ったらこれ
  - GPL: 派生物もオープンソースにする義務。コミュニティ貢献を重視する場合
  - Apache 2.0: 特許条項あり。企業との共同研究がある場合
  - 論文ジャーナルのrequirement（Data Availability Statement）との関係を確認する

> **🧬 コラム：バイオインフォツール公開の実例**
>
> 多くのバイオインフォ論文では GitHub + Zenodo + DOI の組み合わせが標準になりつつある。
>
> **典型的な公開フロー:**
> 1. GitHubでコードを開発・公開
> 2. Zenodo連携を有効にする
> 3. GitHub Releasesでバージョンをタグ付け → Zenodoが自動でDOIを発行
> 4. 論文のData Availability Statementに DOI を記載
>
> **conda/PyPI公開との関係:**
> - DOI = 論文からの引用用（永続的なスナップショット）
> - PyPI/bioconda = ユーザーがインストールする用（最新版）
> - 両方を用意するのがベストプラクティス（§10-6の成長パスも参照）
>
> → 投稿前の最終確認は[付録C](./appendix_c_checklist.md)を参照

### 7-3. コミットの作法
- 意味のあるコミットメッセージ（Conventional Commits）
- 1コミット = 1つの論理的変更
- 大きなファイルの扱い（Git LFS）
- 大規模データの共有 — Git LFS を超える選択肢（Hugging Face Hub, Zenodo, クラウドストレージ）
- チェックサムによるデータ整合性の検証（`sha256sum`）

### 7-4. セマンティックバージョニング
- `MAJOR.MINOR.PATCH`（例: `1.2.3`）
- 破壊的変更、機能追加、バグ修正の区別
- CHANGELOG の書き方

---

## 8. コードの正しさを守るテスト技法

### 8-1. テスト駆動開発（TDD）
- Red → Green → Refactor サイクル
- テストを先に書くことの利点
- 「テストが通る最小限のコード」を書く規律

### 8-2. テストの実践
- **pytest** によるユニットテスト
- テストデータの準備（fixture）
- エッジケースを意識する（空入力、巨大入力、不正入力）
- テストカバレッジの計測（`pytest-cov`）
- 統合テスト・回帰テスト

### 8-3. コード品質ツール
- **ruff** — リント＋フォーマット
- **mypy** — 型チェック
- **pre-commit** — コミット前の自動チェック
- docstringの書き方（Google style / NumPy style）
- **エージェントフック** — ツール実行前後の自動チェック（pre-commitとの使い分け、設定例、バイオインフォでの活用例）

### 8-4. CI/CD
- GitHub Actionsの基礎
- テスト・リンターの自動実行
- ワークフローYAMLの書き方

> **🧬 コラム：バイオインフォのテストデータ**
>
> テスト用に小さなFASTA/BAM/VCFを用意する。本番データ（数十GB）はテストに使わない。
> - `seqtk sample reads.fq 1000 > test_reads.fq` でサブサンプル作成
> - `samtools view -s 0.01 input.bam > test.bam` で1%に間引き
> - テストデータは `tests/data/` に置き、リポジトリにコミットする

---

## 9. デバッグの技術 — tracebackから最小再現例まで

### 9-1. デバッグの心構え
- tracebackを最後の行から読む
- 「何を期待して何が違ったか」を言語化する
- 最小再現例（MRE）を作る
- 二分探索デバッグ（問題箇所を半分ずつ絞り込む）

### 9-2. デバッグツール
- `print()` デバッグ
- `pdb` / `breakpoint()` / `ipdb`

### 9-3. よくあるバグのパターン
- Off-by-one（0-based vs 1-based）
- 相対パス vs 絶対パス
- エンコーディング（UTF-8）
- 型の不一致（str vs int）
- メモリ不足（OOM）
- 並列処理の競合状態
- Python固有の落とし穴（ミュータブルデフォルト引数、浅いコピーvs深いコピー、is vs ==、SettingWithCopyWarning、NumPyビューvsコピー）

---

## 10. ソフトウェア成果物の設計 — スクリプトからパッケージまで

開発を始める前に「**最終的にどういう形で提供するのか**」を決める。
これを決めないまま AIエージェントに「作って」と言うと、的外れなプロジェクト構成が生成される。
CLAUDE.md / AGENTS.md に「このプロジェクトはpipインストール可能なライブラリとして公開する」と明記するだけで、
生成されるコードの構造が大きく変わる。

### 10-1. 成果物の形式の選択

以下のパターンから、自分の目的に合ったものを選ぶ。
複数を組み合わせることもある（例: ライブラリ＋CLIエントリポイント）。

#### パターン1: スタンドアロンスクリプト

**概要:** 1つの `.py` ファイルで完結する。最も手軽。

**向いている場面:**
- 一回きりの解析、使い捨てスクリプト
- 自分しか使わない前処理・変換スクリプト
- 数百行以内の小さな処理

**構成例:**
```
filter_vcf.py          # スクリプト本体
README.md              # 使い方
```

**実行方法:** `python filter_vcf.py --input variants.vcf --min-qual 30`

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトは単一のPythonスクリプト（filter_vcf.py）。
パッケージ化はしない。argparseでCLIを提供する。
```

**注意:** 大きくなったらライブラリ化を検討する。500行を超えたらほぼ確実に分割すべき。

---

#### パターン2: pipインストール可能なPythonパッケージ

**概要:** `pip install` でインストールでき、`import mypackage` で使えるライブラリ。
CLIエントリポイントも同時に提供できる。

**向いている場面:**
- 他の人にも使ってほしいツール
- 複数のモジュールに分かれる中〜大規模なプロジェクト
- PyPIで公開したい場合

**構成例:**
```
my_tool/
├── src/my_tool/
│   ├── __init__.py
│   ├── io.py
│   ├── analysis.py
│   ├── cli.py           # CLIエントリポイント
│   └── viz.py
├── tests/
├── pyproject.toml        # ★ これが核心
├── README.md
├── LICENSE
└── .gitignore
```

**核心となる `pyproject.toml`:**
```toml
[project]
name = "my-tool"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "biopython>=1.80",
    "pysam>=0.22",
    "click>=8.0",
]

[project.scripts]
my-tool = "my_tool.cli:main"    # CLIコマンドの登録
```

**インストールと実行:**
```bash
pip install .                     # ローカルインストール
pip install -e .                  # 開発モード（コード変更が即反映）
my-tool align --input reads.fq   # CLIとして実行
```

```python
from my_tool.analysis import run_analysis   # ライブラリとして利用
```

**公開:**
```bash
pip install build twine
python -m build                   # sdist + wheel を生成
twine upload dist/*               # PyPIにアップロード
```

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはpipインストール可能なPythonパッケージ。
src layoutを使う。CLIエントリポイントはclickで実装。
pyproject.tomlで依存関係を管理する。
```

---

#### パターン3: condaパッケージ

**概要:** `conda install` でインストールできる形式。バイオインフォではbiocondaチャネルで公開するのがデファクト。

**向いている場面:**
- C/C++の依存（htslib等）がありpipだけでは解決しにくいとき
- biocondaエコシステムの一部として公開したいとき
- conda環境での再現性を重視するとき

**追加で必要なもの:**
- `meta.yaml`（condaビルドレシピ）
- biocondaへのPR（bioconda-recipesリポジトリ）

**実装ステップ:**
1. まずpipインストール可能なパッケージ（パターン2）を完成させる
2. PyPIに公開する
3. `meta.yaml` を書いてbioconda-recipesにPRを出す

**注意:** condaパッケージ化はパターン2の上に成り立つ。いきなりcondaだけを目指さない。

---

#### パターン4: シェルスクリプトで繋いだパイプライン

**概要:** 複数のコマンド（既存ツール＋自作スクリプト）をシェルスクリプトでつなぐ。

**向いている場面:**
- 既存ツール（samtools, STAR, featureCounts等）を順番に実行する定型パイプライン
- 手動で毎回コマンドを打つのが面倒になったとき
- ワークフロー言語を導入するほど複雑でない場合

**構成例:**
```
pipeline/
├── run_rnaseq.sh         # メインのパイプラインスクリプト
├── scripts/
│   ├── filter_lowqual.py # 自作の前処理スクリプト
│   └── summarize.py      # 結果集計
├── config.sh             # パラメータ設定
└── README.md
```

**実装のコツ:**
- `set -euo pipefail` を必ず書く（エラー時に即停止）
- パラメータは `config.sh` で外部化する
- ログをファイルに保存する（`2>&1 | tee log.txt`）
- 各ステップの成功/失敗をチェックする

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはBashパイプライン。scripts/にPython補助スクリプトを置く。
config.shでパラメータを管理する。set -euo pipefail必須。
```

**限界:** サンプル数が多い場合や条件分岐が複雑な場合はワークフロー言語（パターン5）に移行する。

---

#### パターン5: ワークフロー言語（Snakemake / Nextflow）

**概要:** 依存関係・並列実行・再開機能を持つ本格的なパイプライン。

**向いている場面:**
- 多サンプル処理（10+サンプル）
- ステップ間の依存関係が複雑
- HPC上での並列実行が必要
- 中間結果からの再開が必要
- 他の研究者が再現実行する想定

**構成例（Snakemake）:**
```
workflow/
├── Snakefile              # ワークフロー定義
├── config/
│   └── config.yaml        # パラメータ
├── rules/
│   ├── mapping.smk        # マッピングルール
│   ├── quantification.smk # 定量ルール
│   └── qc.smk            # QCルール
├── scripts/
│   ├── deseq2.py         # カスタム解析スクリプト
│   └── plot_volcano.py
├── envs/
│   └── mapping.yaml       # conda環境定義
└── README.md
```

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはSnakemakeワークフロー。
ルールはrules/に分割する。各ルールにconda環境を指定する。
configはconfig/config.yamlで管理する。
```

**パターン4からの移行サイン:**
- サンプルごとにコマンドをコピペしている → ワイルドカードで解決
- 途中で失敗したら最初からやり直し → `--rerun-incomplete` で途中再開
- HPCのジョブ管理が手動 → `--cluster`/`--slurm` で自動化

---

#### パターン6: Dockerコンテナ / Apptainerイメージ

**概要:** 実行環境ごとパッケージングして配布する。

**向いている場面:**
- 依存関係が複雑で、インストール手順が再現しにくい
- 異なる環境（ローカル、HPC、クラウド）で同一の実行環境を保証したい
- 論文の再現性を完全に保証したい

**他のパターンとの組み合わせ:**
- パターン2（pipパッケージ）をDockerでラップ
- パターン5（Snakemake）の各ルールにコンテナを指定（`container:`ディレクティブ）

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはDockerコンテナとして配布する。
ベースイメージはmambaforge。Dockerfileをルートに置く。
```

---

#### パターン7: Jupyter Notebook（解析レポート）

**概要:** コード・実行結果・解説文をまとめた対話的ドキュメント。

**向いている場面:**
- 探索的データ解析（EDA）
- 解析結果のレポート・共有
- チュートリアルやデモ

**注意:**
- Notebookは**解析の最終成果物**としては使える
- しかし**再利用可能なツール**としてはNotebookを公開しない
  - ロジックが固まったら `.py` に抽出してライブラリ化する
  - Notebookは「呼ぶ側」にとどめる

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはJupyter Notebookによる解析レポート。
共通ロジックはsrc/に抽出し、NotebookからはimportしてGlueコードのみ書く。
```

---

#### パターン8: Webアプリ（Streamlit / Gradio）

**概要:** 非プログラマーでもブラウザから使えるGUIを提供する。

**向いている場面:**
- 共同研究者（実験系）に解析ツールを使ってもらいたいとき
- パラメータを変えながらインタラクティブに結果を確認したいとき
- デモやプレゼンテーション

**構成例:**
```
my_app/
├── app.py                # Streamlitアプリ本体
├── src/my_tool/          # ロジック（パターン2と同じ）
│   ├── analysis.py
│   └── viz.py
├── pyproject.toml
├── requirements.txt
└── README.md
```

**実装のコツ:**
- **ロジックとUIを分離する** — `app.py` はGlueコードのみ。解析ロジックは `src/` に置く
- Streamlitは `pip install streamlit` → `streamlit run app.py` で起動
- Gradioは機械学習モデルのデモに特に適している
- HPC上で動かす場合はポートフォワーディングが必要（§2-5参照）

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはStreamlitアプリ。app.pyにUIコードを書き、
解析ロジックはsrc/my_tool/に分離する。UIとロジックを混ぜない。
```

**注意:** Webアプリは「届け方」であり「作り方」ではない。
まずパターン2でロジックを作り、それをStreamlitで包む、という順序で進める。

---

#### パターン9: コンパイル済みバイナリ

**概要:** Pythonがインストールされていない環境でも動く実行ファイルとして配布する。

**向いている場面:**
- 配布先にPython環境がない（稀だがある）
- ユーザーにpipやcondaの操作を求めたくないとき
- GitHub Releasesでワンバイナリを配布したいとき

**ツール:**
- **PyInstaller** — 最も広く使われる。`pyinstaller --onefile my_tool.py` で単一バイナリ生成
- **Nuitka** — PythonコードをCにコンパイル。PyInstallerより高速な実行ファイルを生成
- **Shiv / PEX** — Python zipappとしてパッケージ（Pythonは必要だがpip不要）

**注意:**
- バイナリサイズが大きくなりがち（数十〜数百MB）
- OS/アーキテクチャごとにビルドが必要（Linux, macOS, Windows）
- GitHub Actionsでクロスプラットフォームビルドを自動化するのがベストプラクティス
- **バイオインフォでは稀なパターン** — 多くの場合condaやコンテナのほうが適切

**プロジェクト設定ファイル（CLAUDE.md / AGENTS.md）への記述例:**
```
このプロジェクトはPyInstallerで単一バイナリとして配布する。
GitHub Actionsでlinux/macOSのバイナリを自動ビルドしてReleasesに添付する。
```

---

### 10-2. どのパターンを選ぶか — 判断フローチャート

```
自分だけが使う一回きりの処理？
  → Yes: パターン1（スクリプト）
  → No ↓

既存ツールを順番に実行するだけ？
  → Yes: サンプル数10以下 → パターン4（シェルスクリプト）
         サンプル数10以上 or HPC → パターン5（ワークフロー言語）
  → No ↓

Pythonで独自のロジックを実装する？
  → Yes: 他の人にも使ってほしい → パターン2（pipパッケージ）
         自分のラボ内で使う    → パターン2（軽量版）
  → No ↓

非プログラマーにGUIで使わせたい？
  → Yes: パターン8（Streamlit）+ ロジックはパターン2に分離

解析結果のレポートを共有したい？
  → Yes: パターン7（Notebook）+ ロジックはパターン2に分離
```

**よくある組み合わせ:**
- **ライブラリ＋CLI＋コンテナ**: パターン2 + パターン6（最も汎用的）
- **ワークフロー＋コンテナ**: パターン5 + パターン6（論文再現性に最適）
- **ライブラリ＋Streamlit**: パターン2 + パターン8（共同研究者向け）
- **ライブラリ＋conda**: パターン2 + パターン3（biocondaエコシステム統合）

**エージェントへの最初の指示に含めるべきこと:**
```
> /plan
> FASTQファイルのクオリティフィルタリングツールを作りたい。
> 成果物の形式: pipインストール可能なPythonパッケージ（CLIつき）
> 対象ユーザー: ラボのメンバー
> インタビューして仕様を固めて。
```

「成果物の形式」を最初に伝えることで、エージェントは適切なプロジェクト構成、
`pyproject.toml`、エントリポイント設計を最初から考慮した計画を立てる。

### 10-3. ディレクトリ構成（パターン2: pipパッケージの標準レイアウト）

パターンごとのディレクトリ例は§10-1で示した。以下はパターン2（pipパッケージ）の典型構成:

#### 設定ファイルの階層構造 — ディレクトリ単位のルール設定
- サブディレクトリごとにCLAUDE.md / AGENTS.mdを配置
- ルール適用順（一般→具体）
- 設定の3スコープ（グローバル→プロジェクト→ディレクトリ）
- メモリ機能によるセッション間の記憶持続
```
my_project/
├── src/my_package/     # メインコード
│   ├── __init__.py
│   ├── io.py           # 入出力
│   ├── analysis.py     # 解析ロジック
│   └── viz.py          # 可視化
├── tests/              # テストコード
├── data/               # テストデータのみ
├── results/            # 出力（.gitignore）
├── pyproject.toml
├── README.md
├── CHANGELOG.md
├── LICENSE
├── Dockerfile
└── .gitignore
```

### 10-4. 設定管理
- ハードコーディングを避ける
- 設定ファイル（YAML / TOML）による外部化
- コマンドライン引数による上書き
- 環境変数による秘密情報の管理
- デフォルト値の設計

### 10-5. エラーハンドリング
- 例外の使い分け（ValueError, FileNotFoundError, カスタム例外）
- 早期リターン（ガード節）パターン
- ユーザーに分かりやすいエラーメッセージ
- fail-fast の原則

### 10-6. 段階的な成長パス — スクリプトから育てる

最初から完璧な形式を目指す必要はない。
多くの研究ツールは以下の段階を経て成熟する:

```
パターン1                 パターン2               パターン2+3
スクリプト ───────→ pip パッケージ ───────→ conda/PyPI公開
 (自分用)           (CLIつき)              (コミュニティへ)
   │                   │                       │
   │                   ├─→ パターン5            ├─→ パターン6
   │                   │   ワークフロー統合       │   コンテナ化
   │                   │                       │
   │                   └─→ パターン8            └─→ 論文公開
   │                       Streamlit GUI            (再現性保証)
   │
   └─→ パターン4
       シェルパイプライン ──→ パターン5
                            ワークフロー言語
```

**成長のタイミングの目安:**
- スクリプトが500行を超えた → パッケージ化を検討
- `python script.py` を何度もコピペしている → CLIエントリポイントを作る
- 「あのスクリプトどこだっけ」が頻発 → `pip install -e .` で開発モード
- 共同研究者から「使い方がわからない」 → README + Streamlit GUI
- 論文投稿時 → Dockerfile + ワークフロー + バージョン固定
- 他のラボから「使いたい」 → PyPI/bioconda公開

**AIエージェントとの連携:**
```
> /plan
> このスクリプト（filter_vcf.py, 600行）をpipインストール可能なパッケージに
> リファクタリングしたい。現在の機能を壊さずに段階的に移行する計画を立てて。
```
エージェントはテストを書きながら段階的にリファクタリングする計画を立ててくれる。
一気に書き換えるより、テストで保護しながら少しずつ移行するのが安全。

---

## 11. コマンドラインツールの設計と実装

### 11-1. コマンドラインインターフェースの作法
- **argparse** / **click** / **typer** の選択
- 必須引数とオプション引数の設計
- サブコマンド設計（`mytool align`, `mytool count`）
- `--help`, `--version`, `--verbose` / `--quiet`
- 入力: ファイル or stdin、出力: ファイル or stdout のUNIX的設計

> **🧬 コラム: バイオインフォCLIツールの設計に学ぶ**
>
> samtools/bedtools/seqkitの設計パターン分析。サブコマンド型設計、stdin/stdout対応、ログ/プログレスはstderrの原則。

> **🧬 コラム: パラメータ爆発問題**
>
> GATKのHaplotypeCallerは50以上のオプション。「頻繁に変えるものだけCLI引数に」「`--preset` パターン」の対処法。

#### カスタムコマンド（Agent Skills） — エージェント向けのテンプレート
- 定型指示の繰り返し問題
- カスタムコマンドの作り方（`.claude/commands/` / `SKILL.md`）
- 実用例（レビュー、テスト実行、コミット準備）
- CLIコマンドとカスタムコマンドの設計原則の対比

### 11-2. プログレス表示とUI
- **tqdm** / **rich** によるプログレスバー
- カラー出力とテーブル表示（rich）
- 処理段階のステータスメッセージ

### 11-3. ロギング
- `print()` vs `logging` モジュール
- ログレベル（DEBUG, INFO, WARNING, ERROR, CRITICAL）
- ログの出力先（stderr, ファイル）
- タイムスタンプの重要性
- `--log-level` オプションの実装

> **🤖 コラム: 機械学習CLIの設計パターン**
>
> Hydraによる YAML設定ベースの引数管理、wandbとの実験追跡統合。

---

## 12. データ処理の実践 — NumPy・pandas・polars

### 12-1. NumPyによるベクトル化演算
- ベクトル化とは何か — forループ vs NumPy演算の速度差
- バイオインフォでの実践: GC含量の一括計算
- ブロードキャスティング — CPM正規化
- 正規化手法の選択基準（CPM, TPM, FPKM/RPKM, DESeq2正規化の使い分け）
- ファンシーインデックスとマスク — Quality scoreフィルタ

### 12-2. pandasとpolarsによるテーブルデータ処理
- **pandas** の基本操作パターン — `read_csv()`, `.query()`, `.groupby().agg()`, `pd.merge()`
- DEG結果テーブルの処理 — 有意遺伝子フィルタ、メタデータ結合、カテゴリ別集計
- メソッドチェーン — `.pipe()`, `.assign()`
- **polars** — lazy evaluation、pandas→polars対応表、使い分けの指針

### 12-3. ライブラリ関数の活用 — AIが再発明しがちなパターン
- 統計検定 — 関数名を指示に含める（`ttest_ind()`, `mannwhitneyu()`）
- 多重検定補正 — 車輪の再発明の典型例（手動実装 vs `false_discovery_control()`）
- 距離行列 — forループ vs `pdist()` + `squareform()`
- SciPyの主要モジュール早見表

> **🧬 コラム：バイオインフォマティクスのPythonライブラリ**
>
> | ライブラリ | 用途 | 代表的な使い方 |
> |-----------|------|---------------|
> | **Biopython** | 配列操作の万能ナイフ | `SeqIO`（FASTA/FASTQ）, `AlignIO`（MSA）, `Blast`（結果パース）, `Entrez`（NCBI API） |
> | **pysam** | SAM/BAM/VCF操作 | htslib のPythonラッパー。高速 |
> | **pyBigWig** | BigWigの読み書き | ゲノムシグナルデータ |
> | **scanpy** | シングルセル解析 | 前処理→クラスタリング→可視化の一気通貫 |
> | **anndata** | シングルセルデータ構造 | `.X`, `.obs`, `.var`, `.obsm` |
> | **pyranges** | ゲノム区間演算 | BED的操作をpandas風に |
> | **ETE Toolkit** | 系統樹操作・可視化 | 系統樹の読み込み、装飾、解析 |
>
> エージェントに実装を頼む前に、これらのライブラリが提供する機能を確認する。
> 例: FASTAのパーサを自作する必要はない → `Bio.SeqIO.parse()` を使う。

> **🧬 コラム：実験デザインの語彙** — バイオロジカルリプリケートvsテクニカルリプリケート、バッチ効果、交絡因子、擬似リプリケート、サンプルサイズ

> **🤖 コラム：機械学習ライブラリ**
>
> バイオインフォ×MLで頻出するライブラリ群。配列解析と同じく車輪の再発明を避ける。
>
> | ライブラリ | 用途 | 代表的な使い方 |
> |-----------|------|---------------|
> | **scikit-learn** | 古典的ML | 分類、クラスタリング、前処理パイプライン、交差検証 |
> | **PyTorch** | 深層学習 | カスタムモデル定義、学習ループ、GPU計算 |
> | **JAX** | 高速数値計算 | 自動微分、JITコンパイル、関数型スタイル |
> | **Hugging Face** | 事前学習モデル | Transformers, Datasets, tokenizers。DNAモデル（DNABERT等）もここにある |
> | **Lightning** | 学習ループの定型化 | PyTorch Lightningでボイラープレート削減 |
> | **wandb** | 実験追跡 | 学習曲線、ハイパーパラメータ、モデルチェックポイント（§15参照） |
> | **optuna** | ハイパラ最適化 | ベイズ最適化ベースの自動チューニング |
>
> 配列解析からMLに踏み出す典型的な流れ:
> 1. scikit-learnで特徴量ベースの古典的MLを試す（まずベースラインを作る）
> 2. PyTorchでカスタムモデルを構築
> 3. Hugging Faceで事前学習モデルのfine-tuning

---

## 13. 可視化の実践 — matplotlib・seaborn・plotly

### 13-1. Python可視化
- **matplotlib** — 基本プロット（Figure/Axesのオブジェクト指向API）
- **seaborn** — 統計的可視化
- **plotly** — インタラクティブグラフ

### 13-2. 可視化の原則
- 適切なグラフ種類の選択（分布→バイオリン、相関→散布図、行列→ヒートマップ）
- 色覚多様性への配慮（viridis, cividis パレット）
- 軸ラベル・凡例・タイトルの必須化
- 出力形式の使い分け（PNG: ラスタ、SVG/PDF: ベクタ、dpi設定）
- 再現可能なプロットスクリプト化

> **🧬 コラム：バイオインフォの専門可視化ツール**
>
> **ゲノムブラウザ**（リードマッピングや変異の確認）
> - **IGV** — デスクトップ定番。BAM, VCF, BED, BigWig対応。バッチスクリプトで自動スクリーンショットも可
> - **UCSC Genome Browser** — ウェブベース。カスタムトラックアップロード
> - **JBrowse 2** — 次世代ウェブブラウザ。構造変異の可視化に強い
>
> **アラインメントビューア**（MSAの確認・編集）
> - **Jalview** — MSA閲覧・編集の定番GUI。カラースキーム、保存度表示、系統樹連動
> - **AliView** — 大規模MSAの高速ビューア。数万配列でも動作
> - **Seaview** — MSA編集＋系統樹構築の統合環境
>
> **系統樹**
> - **FigTree** — Newick/Nexus系統樹のGUIビューア
> - **iTOL** — ウェブベース。注釈・装飾が豊富
> - **ETE Toolkit** — Pythonからプログラマティックに操作
>
> **ゲノムトラック描画**（論文用の図）
> - **pyGenomeTracks** — マルチトラック図をINI設定で生成
> - **deepTools** — BAM/BigWigのシグナルヒートマップ

> **🤖 コラム：機械学習の可視化ツール**
>
> MLプロジェクトでは学習過程やモデル性能の可視化が必要になる。
>
> | 場面 | ツール | 用途 |
> |-----|-------|------|
> | 学習曲線の追跡 | **wandb** / **TensorBoard** | loss, accuracy等のリアルタイム可視化 |
> | モデル性能評価 | **scikit-learn** metrics + matplotlib | confusion matrix, ROC曲線, precision-recall |
> | 次元削減の可視化 | **UMAP** / **t-SNE** (scanpy, sklearn) | 埋め込み空間のクラスタ構造の確認 |
> | attention可視化 | **bertviz** / カスタム描画 | Transformerモデルの注意機構の解釈 |
>
> ポイント: wandbやTensorBoardはリモートHPC上の学習を手元のブラウザから監視できる
> （ポートフォワーディング §2-5 を使う）

---

## 14. 解析パイプラインの自動化 — Snakemake・Nextflow

> 📖 原稿: [14_workflow.md](./14_workflow.md)

### 14-1. なぜワークフロー言語が必要か
- シェルスクリプトの限界（多段階パイプラインの再現性）
- 依存関係の自動解決 — DAGとしてのワークフロー
- 導入タイミング
- QCメトリクスの読み方（FastQC, MultiQC, samtools flagstat）
- QCツールとトリミングパラメータの選択基準（fastp vs FastQC+Trimmomatic、Q20 vs Q30）

### 14-2. ワークフロー言語
- **Snakemake** — Pythonベース。rule, input, output, wildcards, conda/container統合
- **Nextflow** — DSL2、チャネルモデル。nf-core パイプライン群
- **make** — 古典的だが小規模パイプラインには今も有用
- ツール選択の判断基準

### 14-3. ワークフローのベストプラクティス
- 入力データは読み取り専用
- 中間ファイルと最終出力の分離
- パラメータの設定ファイル化
- ログの保存
- `--dry-run` / `--dag` による事前確認
- テスト用の小規模データ
- ベストプラクティスの自動チェック

---

## 15. コンテナによるソフトウェア環境の再現 — Docker・Apptainer

> 📖 原稿: [15_container.md](./15_container.md)

### 15-1. なぜコンテナが必要か
- 「自分の環境では動く」問題の具体例
- 共有ライブラリの問題（§5参照）
- conda/venvとコンテナの違い（Pythonパッケージ隔離 vs. OSレベル隔離）
- 再現性危機への言及
- コンテナの仕組み — 仮想マシンとの違い（カーネル共有、名前空間隔離）
- コンテナを使うべきタイミングの判断基準

### 15-2. Docker
- イメージとコンテナの違い（クラスとインスタンスのアナロジー）
- Dockerfileの書き方（`FROM`, `RUN`, `COPY`, `ENTRYPOINT`）
- ベースイメージの選択（mambaforge推奨）
- レイヤーキャッシュの仕組みと最適化
- マルチステージビルド
- ボリュームマウント（`-v`）
- `docker compose`
- エージェントとDockerの連携（セキュリティ制約、承認モードでの反復構築）
- 🧬 コラム: BioContainersとbioconda

### 15-3. Apptainer / Singularity（HPC向け）
- HPCでDockerが使えない理由（root権限）
- Apptainerの特徴（ユーザー権限実行、`.sif`ファイル、ホストFS自動マウント）
- Dockerイメージからの変換（`apptainer pull docker://`）
- Docker vs. Apptainer コマンド対照表
- GPUコンテナ（`--nv`）
- 🤖 コラム: GPUコンテナとCUDAバージョンの罠

### 15-4. 再現性のレベル
- 4段階のスペクトラム（README記述 → lockファイル → Dockerfile → ダイジェスト固定+WF）
- コスト vs. 再現性の比較
- 両方を併用する戦略（condaのlockファイルをDockerfile内でも使う）

### 15-5. 論文投稿時の再現性パッケージング
- **三点セットの原則**: コンテナイメージ + ワークフロー（§14） + バージョン固定で、第三者が再現可能な環境を提供する
- **コンテナイメージの公開**: Docker Hub / GHCR、タグ戦略、ダイジェスト固定
- **バージョン固定の実践**: `conda-lock` / `pip freeze` / `uv.lock` をDockerfile内で活用
- **論文レビュー対応**: READMEテンプレート、テストデータ同梱
- 再現性パッケージのディレクトリ構成例
- `validate_dockerfile.py` の紹介と使い方

> → 投稿前の最終確認は[付録C](./appendix_c_checklist.md)を参照

### 15-6. 🤖 実験管理（ML/計算実験の追跡）
- なぜ実験管理が必要か — パラメータ・メトリクス・チェックポイントの一元管理
- 実験追跡ツール（wandb, MLflow, hydra, DVC）
- 最小限の実験管理パターン — 自前JSONLログから段階的に導入

---

## 16. スパコン・クラスタでの大規模計算

### 16-1. HPCの基本
- ログインノード vs 計算ノード（ログインノードで重い処理をしない）
- キュー / パーティション
- リソース申請（ノード、CPU、メモリ、GPU）

### 16-2. SLURM
- `sbatch`, `squeue`, `scancel`, `sinfo`, `sacct`
- ジョブスクリプト（`#SBATCH` ディレクティブ）
- アレイジョブ（`--array`）、依存ジョブ（`--dependency`）
- GPU申請（`--gres=gpu:1`）
- インタラクティブジョブ（`srun --pty bash`）

### 16-3. リモート接続とファイル転送
- `rsync` によるファイル転送（ローカル↔HPC間の同期）
- ポートフォワーディング（JupyterやTensorBoardへのアクセス）
- 多段SSH（ProxyJump）— 踏み台サーバ経由での接続
- → SSH基礎は[§2 コラム](#2-ターミナルとシェルの基本操作)を参照

### 16-4. データ管理・バックアップ・共有
- ホーム vs スクラッチの使い分け
  - スクラッチは一時領域であり保管場所ではない — 定期的にパージされる
- モジュールシステム（`module load`）
- 共有ファイルシステムの注意（大量の小ファイルを避ける）
- **3-2-1バックアップルール**
  - 3コピー、2種類のメディア、1つはオフサイト
  - 研究データは失われると取り返しがつかない — バックアップは保険ではなく必須
- **ストレージの使い分け**
  - ラボ内NAS: 中長期保管、グループ共有
  - クラウドストレージ（Google Drive, AWS S3等）: オフサイトバックアップ、外部共有
  - HPC: 計算実行時の一時利用
- **将来のグループメンバーがアクセスできる設計**
  - ディレクトリ命名規則（`YYYYMMDD_project_name/` 等）を統一する
  - 各プロジェクトディレクトリにREADMEを設置し、データの概要・取得元・処理状況を記録する
  - 個人アカウント依存を避ける — 共有アカウントまたは組織アカウントで管理する
  - 学生・ポスドクの異動後もデータにアクセスできるようにする

> → 投稿前の最終確認は[付録C](./appendix_c_checklist.md)を参照

> **🤖 コラム：GPU学習ジョブのSLURMスクリプト**
>
> ```bash
> #!/bin/bash
> #SBATCH --job-name=train_model
> #SBATCH --partition=gpu
> #SBATCH --gres=gpu:1              # GPU 1枚
> #SBATCH --cpus-per-task=8         # データローダー用のCPU
> #SBATCH --mem=32G
> #SBATCH --time=24:00:00
> #SBATCH --output=logs/%x_%j.out
> #SBATCH --error=logs/%x_%j.err
>
> module load cuda/12.1
> source activate ml-env
>
> # GPU状態の記録（デバッグ用）
> nvidia-smi
>
> python train.py \
>     --epochs 100 \
>     --batch-size 64 \
>     --learning-rate 1e-4 \
>     --wandb-project my-project     # 実験追跡（§15）
> ```
>
> **よくあるポイント:**
> - `--cpus-per-task` はデータローダーの `num_workers` に合わせる
> - `nvidia-smi` をジョブ冒頭で呼んでGPUの認識を確認する習慣
> - OOM（GPU メモリ不足）→ バッチサイズを下げる or gradient accumulation
> - マルチGPUは `--gres=gpu:2` + PyTorch DDP（上級者向け）

---

## 17. コードのパフォーマンス改善 — プロファイリングと高速化

### 17-1. プロファイリング
- `time` コマンド、Python `cProfile`, `line_profiler`
- メモリプロファイリング（`memory_profiler`）
- ボトルネックの特定
- 🤖 GPU利用率モニタリング: `nvidia-smi`, `watch -n 1 nvidia-smi`、`gpustat`
- 🤖 GPU メモリ使用量: `torch.cuda.memory_summary()` で詳細確認

### 17-2. 高速化テクニック
- ベクトル化（NumPy）— forループを避ける
- プロセスとスレッドの違い、GIL
- 並列処理（`multiprocessing`, `concurrent.futures`）
- ジェネレータによるメモリ効率化
- 適切なデータ構造の選択
- Cython, Numba（上級）

### 17-3. 大規模データの扱い
- ストリーミング処理（全データをメモリに載せない）
- チャンク処理（pandas `chunksize`）
- 適切なファイル形式の選択（Parquet vs CSV、BAM vs CRAM）
- 圧縮ファイルの直接読み込み（`gzip.open`）

---

## 18. コードのドキュメント化

### 18-1. Markdownの習得
- 見出し、リスト、コードブロック、テーブル
- GitHub Flavored Markdown
- Mermaid記法による図の埋め込み
- 文芸的プログラミング（Literate Programming）— Knuth (1984) の思想
  - 「人間が読むドキュメントの中にコードを埋め込む」
  - Jupyter Notebook、R Markdown、Quarto がこの系譜
  - コードとドキュメントを一体化するアプローチの利点と限界

### 18-2. プロジェクトドキュメント
- **README.md** — 何をするツールか、インストール、使い方、例
- **CHANGELOG.md** — 変更履歴
- docstringからのAPI自動生成（Sphinx, mkdocs）

#### 公開リポジトリのドキュメント
論文に伴ってコードを公開する場合、READMEには以下の情報を含める:
- **目的**: このリポジトリが何をするものか（論文タイトル・DOIとの対応）
- **依存関係**: 必要なソフトウェアとそのバージョン
- **実行手順**: テストデータで結果を再現する具体的な手順
- **データの入手方法**: 生データの取得先（GEO/SRAアクセッション番号等）
- **引用方法**: CITATION.cff へのリンク、または推奨する引用形式
- **LICENSE**: 再利用可能性の明示（§7-2参照）

> → 投稿前の最終確認は[付録C](./appendix_c_checklist.md)を参照

### 18-3. 研究ノート
- 解析の再現に必要な情報（パラメータ、バージョン、環境）の記録
- Jupyter Notebook の利点と限界

---

## 19. 公共データベースとAPI — データ取得の実践

### 19-0. データベースの基礎知識 — エージェントと「同じ言語」で話すために
- **DB種類と用語**: リレーショナルDB(RDB)とSQL（テーブル・SELECT/JOINの概念）、NoSQL（キーバリュー/ドキュメント/グラフ型の概要）、一次DB vs 二次DB、データベース/データマート/データストア/データレイクの整理
- **オントロジーとセマンティックWeb**: オントロジーの概念、RDF（主語-述語-目的語のトリプル）、Linked Data（URIでDB間を接続）、SPARQL（RDFへの問い合わせ言語、SQLとの対比）
- **Vibe codingとの接続**: SQLかSPARQLかREST APIかを指定する語彙、エージェント生成クエリの検証力（JOINの方向、フィルタ条件の妥当性）

> **🧬 コラム：Gene OntologyとKEGG**
>
> - **Gene Ontology (GO)** — DAG構造、エンリッチメント解析での利用
> - **KEGG** — パスウェイDB、API、ライセンス注意
> - **その他のオントロジー** — Sequence Ontology (SO)、Disease Ontology (DO)、Cell Ontology (CL)

### 19-1. バイオインフォマティクスの主要データベース
- **主要DBの全体像**: NCBI系(GenBank/SRA/GEO/PubMed/dbSNP)、欧州系(Ensembl/UniProt/EBI)、日本系(DDBJ/PDBj)、ゲノムブラウザ(UCSC/Ensembl/IGV)、統合系(TogoWS)
- **DB選択の判断力**: 配列→GenBank、生リード→SRA、発現行列→GEO、タンパク質→UniProt。エージェントに指示する前に人間が適切なDBを選ぶ
- **IDの体系**: アクセッション番号の読み方(GSE/GSM/SRR/NM_/ENSG...)、ID変換ツール(biomaRt/MyGene.info/UniProt ID mapping)、エージェントにID変換を依頼する際の注意点

### 19-2. APIによるデータ取得
- **API firstの原則**: スクレイピングや全データDL前に公式APIの有無を確認する習慣。エージェントへの指示と公式ドキュメントでの裏付け
- **HTTPの仕組み**: リクエスト/レスポンスモデル、URL構造（スキーム・ホスト・パス・クエリパラメータ）、ステータスコード（200, 404, 429, 500）
- **REST APIの基礎**: HTTP GET/POST、エンドポイント、レスポンス形式(JSON/XML/FASTA/TSV)、レート制限とAPIキー(→§10参照)
- **NCBI Entrez API**: esearch→efetch→elinkフロー、Biopython `Entrez`モジュール
- **SPARQLエンドポイント**: UniProt SPARQL、TogoStanza。エージェントにSPARQLクエリを生成させる実例
- **Pythonライブラリ**: `requests`+`json`、`Biopython`(Entrez/SeqIO)、`bioservices`

> **🧬 コラム：NCBI系コマンドラインツール**
>
> - **Entrez Direct (EDirect)** — シェルからNCBIを検索。パイプラインで繋げる:
>   `esearch -db nucleotide -query "..." | efetch -format fasta`
> - **SRA Toolkit** — `prefetch` + `fasterq-dump` でリードデータ取得
> - **NCBI Datasets CLI** — ゲノム・遺伝子の一括ダウンロード
> - **BLAST+** — `makeblastdb` でローカルDB作成、`blastn`/`blastp` で検索

> **🧬 コラム：Entrez以外の主要バイオインフォマティクスAPI**
>
> - **Ensembl REST API** — ゲノムアノテーション・バリアント情報をJSONで取得。認証不要
> - **Ensembl BioMart** — 大量IDの一括取得・変換に特化。`pybiomart`/`biomaRt`
> - **UCSC Genome Browser REST API** — トラックデータへのプログラムアクセス
> - **TogoWS** — 複数DBへの統一RESTゲートウェイ(`/entry/DB名/ID`)
> - **MyGene.info** — 遺伝子情報検索・ID変換のREST API

> **🧬 コラム: バイオインフォマティクス向けMCPサーバーとAIエージェント**
>
> バイオ系MCPサーバー（BioMCP、PubMed MCP、GEOmcp、UniProt MCP、gget-MCP）の紹介と活用例。
> Biomni（Stanford大）— 150+ツール・59DB統合のバイオメディカルAIエージェント。MCPサーバーとの違いと併用。

### 19-3. データのダウンロードとローカル管理
- **大規模データのダウンロード**: wget/curl（リトライ・帯域制限）、SRA Toolkit(prefetch+fasterq-dump)、Aspera(ascp)、エージェントにダウンロードスクリプトの一括生成を依頼
- **チェックサム検証**: md5sum/sha256sum、自動検証スクリプト
- **ローカルDB構築**: BLASTデータベース(makeblastdb)、ゲノムインデックス(BWA/STAR/HISAT2)、SQLiteによるメタデータ管理(→§4接続)

> **🧬 コラム：データベースへの登録 — Vibe codingで登録準備を効率化する**
>
> - **登録全体フロー**: DB選択→メタデータ整備→ファイル準備→アップロード→公開設定
> - **エージェントに依頼できること**: GEOテンプレート変換スクリプト、ファイル名-メタデータ整合性チェック、FTPアップロードスクリプト
> - **人間が判断すること**: メタデータの科学的正確性、公開範囲・タイミング、利用規約
> - **DOI付き登録**: Zenodo(GitHub連携→§7-2)、figshare、使い分け
> - → メタデータ設計は§4-0参照、投稿前チェックは[付録C](./appendix_c_checklist.md)参照

---

## 20. コードとデータのセキュリティ・倫理

### 20-1. セキュリティの基礎
- 19-1-1. シークレット管理の原則 — ライフサイクル（生成→保管→利用→ローテーション→失効）、`.env` + `python-dotenv`、git-secrets/pre-commit hook、SSH鍵のパスフレーズ
- 19-1-2. AIエージェントのセキュリティとハーネス — 最小権限の原則、サンドボックス・ネットワーク制限・コマンドホワイトリスト・監査ログ、プロンプトインジェクション対策
- 19-1-3. 依存パッケージのセキュリティ — サプライチェーン攻撃（タイポスクワッティング）、pip-auditによる脆弱性スキャン
- 🧬 コラム: プロンプトインジェクションとバイオインフォマティクス

### 20-2. データの倫理
- 19-2-1. ヒトデータの法規制と利用規約 — 制限付きDB（dbGaP/JGA/EGA）、個人情報保護法/次世代医療基盤法/GDPR/HIPAA、DUAの読み方、AIエージェントへのヒトデータ送信リスク、ローカルLLMエージェントの活用
- 19-2-2. 匿名化と再識別リスク — ゲノムデータの特殊性、k-匿名性、メタデータ粒度制御、合成データの活用
- 19-2-3. 研究倫理とデータ管理計画 — IRB、DMP、オープンサイエンスと個人情報保護のバランス
- 🧬 コラム: dbGaPの利用規約違反とその教訓
- 🧬 コラム: ゲノムデータは「究極の個人情報」

---

## 21. 共同開発の実践 — レビュー・質問・OSS参加

> 📖 原稿: [21_collaboration.md](./21_collaboration.md)

### 21-1. 質問の技術 — 正しく助けを求める
- 20-1-1. 良い質問の構成要素 — 環境情報、最小再現例（MRE）、期待と実際の差分
- 20-1-2. 質問先の使い分け — Stack Overflow / Biostars / GitHub Issues / 研究室Slack
- 20-1-3. エラーメッセージでの検索習慣とXY問題の回避
- 🧬 コラム: Biostarsの歩き方 — バイオインフォの質問文化

### 21-2. コードレビューとOSSコミュニティへの参加
- 20-2-1. コードレビューのコミュニケーション — 建設的なフィードバックの書き方、レビューを受けた際の対応
- 20-2-2. OSSコミュニティへの最初の貢献 — typo修正PR、バグ報告Issue、CONTRIBUTING.mdの読み方
- 20-2-3. バイオインフォOSSの特徴 — アカデミア主導・少人数メンテナ、biocondaレシピ貢献
- 20-2-4. 貢献時のライセンス — CLA署名、他者コード取り込み時の注意（選択基準は§7-2参照）

### 21-3. 共同研究のコミュニケーション — PIと共同研究者への共有
- 20-3-1. 進捗共有の資料作成 — git logからの変更履歴要約、週次報告テンプレート
- 20-3-2. 実験系研究者との橋渡し — 解析結果を非専門家に伝える書き方、パラメータ選択根拠の記録
- 20-3-3. 解析依頼の受け取り方 — 必要情報のチェックリスト化、「解析前インタビュー」パターン（§0-2の応用）
- 🧬 コラム: 研究室ミーティングでの解析報告 — 図1枚で伝える技術

---

## 付録A. AIコーディングエージェントとの効果的な学習パターン

エージェントと人間の役割分担を、エビデンスに基づいて整理する。

### A-1. なぜ役割を分けるのか
- A-1-1. 生産性と品質のトレードオフ — AI支援による速度向上と品質保証の乖離
- A-1-2. ドメイン知識の壁 — 汎用コード生成とバイオ固有の判断
- A-1-3. 認知的オフローディングと学習効果 — 即時補助と長期スキル習得の分離

### A-2. 役割分担の3つの軸
- 自動化可能性・ドメイン依存度・学習効果の3軸による判断基準

### A-3. 学習パターン一覧（14パターン）
- 各パターンに根拠列（3軸のいずれか）を追加した一覧表

---

## 付録B. Claude Code CLI / Codex CLI クイックリファレンス対照表

> 本付録の記述は 2026年3月時点の各ツールの仕様に基づく。最新の仕様は公式ドキュメント（Claude Code: docs.anthropic.com、Codex CLI: github.com/openai/codex）を参照。

概念で理解し、ツール固有の操作は以下を参照する。

### セットアップ

| | Claude Code CLI | Codex CLI |
|--|----------------|-----------|
| インストール | `npm install -g @anthropic-ai/claude-code` | `npm install -g @openai/codex` |
| 起動 | `claude` | `codex` |
| 認証 | Claude Pro/Max or APIキー | ChatGPT Plus/Pro or APIキー |
| プロジェクト設定 | `CLAUDE.md` | `AGENTS.md` |
| ユーザー設定 | `~/.claude/` | `~/.codex/config.toml` |

### 承認モード

| 安全レベル | Claude Code | Codex CLI |
|-----------|-------------|-----------|
| 読み取り専用 | Plan Mode (`Shift+Tab`×2 / `/plan`) | `-s read-only` |
| 承認あり（デフォルト） | Normal Mode | Auto（デフォルト） |
| 全自動 | Auto-Accept Mode (`Shift+Tab`) | `--full-auto` |
| 権限設定 | `/permissions` | `sandbox_mode` in config.toml |

### モデルと推論

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| 最高精度 | Opus 4.6 | GPT-5.4 |
| バランス | Sonnet 4.6 | GPT-5.3-Codex |
| 高速・低コスト | Haiku 4.5 | GPT-5.3-Codex-Spark |
| モデル切替 | `/model` | `/model` |
| 推論の深さ | Extended Thinking (`Alt+T` / `Option+T`) | Reasoning Effort (None〜Extra High) |
| 推論深さ設定 | Opus 4.6: adaptive（自動調節） | `-c model_reasoning_effort="high"` |
| 計画時の推論 | Plan Mode + Extended Thinking | `plan_mode_reasoning_effort` |

### セッション管理

| 操作 | Claude Code | Codex CLI |
|-----|-------------|-----------|
| コンテキスト圧縮 | `/compact` | （自動管理） |
| セッション再開 | `/resume` | `codex resume` / `codex resume --last` |
| Undo | `Esc Esc` (`/rewind`) | git revertで対応 |
| 計画をエディタで編集 | `Ctrl+G` | — |
| 非対話実行 | `claude -p "..."` | `codex exec "..."` |
| MCP追加 | `claude mcp add` | `codex mcp add` |

### カスタマイズ

| | Claude Code | Codex CLI |
|--|-------------|-----------|
| カスタムコマンド | `.claude/commands/` にMDファイル | `$skill-name`（SKILL.mdベース） |
| フック | `.claude/hooks/` | hooks in config.toml |
| MCP統合 | `claude mcp add` | `codex mcp add` |
| プロファイル | — | `--profile dev` (config.tomlの`[profiles]`) |

---

## 付録C. 論文投稿前チェックリスト

各章で学んだ「論文公開を見据えた作法」を、投稿前に一覧で総点検するためのチェックリスト。
各項目から該当章への相互参照リンクを付けているので、詳細はそちらを参照する。

### 1. コード

- [ ] GitHubリポジトリが公開設定になっている
- [ ] ライセンスファイル（LICENSE）がある — [§7-2](#7-2-githubの活用)
- [ ] CITATION.cff がある（GitHubの「Cite this repository」ボタンが機能する） — [§7-2](#7-2-githubの活用)
- [ ] GitHub Releasesでバージョンがタグ付けされている — [§7-2](#7-2-githubの活用)
- [ ] Zenodo連携でDOIが発行されている — [§7-2](#7-2-githubの活用)
- [ ] READMEに目的・依存関係・実行手順・データ入手方法・引用方法が書かれている — [§18-2](#18-2-プロジェクトドキュメント)

### 2. データ

- [ ] 生データがGEO/SRAに登録されている（アクセッション番号を取得済み） — [§19-3 コラム](#19-3-データのダウンロードとローカル管理)
- [ ] メタデータが機械可読形式（TSV/CSV）で整備されている — [§4-0](./04_data_formats.md)
- [ ] 中間・最終データがZenodo/figshareに登録されている（必要に応じて） — [§19-3 コラム](#19-3-データのダウンロードとローカル管理)
- [ ] embargo期間が適切に設定されている — [§19-3 コラム](#19-3-データのダウンロードとローカル管理)
- [ ] データファイル（TSV/CSV/FASTA）に全角スペース・全角英数字が混入していない — [§4 コラム](#-コラム-日本語環境の罠--shift_jis-bom-nfd-マルチバイト文字)
- [ ] ダウンロードデータのチェックサム（MD5/SHA256）を検証している — [§19-3](#19-3-データのダウンロードとローカル管理)

### 3. 再現性

- [ ] Dockerfile / environment.yml がリポジトリに含まれている — [§15](#15-コンテナによるソフトウェア環境の再現--dockerapptainer)
- [ ] 依存パッケージのバージョンが固定されている（lockファイル） — [§6](#6-python環境の構築--pyenvvenvcondauv)
- [ ] ワークフローが自動実行可能（Snakemake/Nextflow/make） — [§14](#14-解析パイプラインの自動化--snakemakenextflow)
- [ ] シード値がコードとメソッドに記載されている — [§3-4](./03_cs_basics.md)
- [ ] 使用したツールのバージョンが記録されている — [§6](#6-python環境の構築--pyenvvenvcondauv)
- [ ] テストデータで全パイプラインを再現する手順がREADMEにある — [§15-5](#15-5-論文投稿時の再現性パッケージング)
- [ ] 可視化が色覚多様性に配慮したカラーパレットを使用している — [§13-2](#13-2-可視化の原則)

### 4. バックアップ・共有

- [ ] データが3-2-1ルールでバックアップされている — [§16-4](#16-4-データ管理バックアップ共有)
- [ ] ラボ共有ストレージにコピーがある — [§16-4](#16-4-データ管理バックアップ共有)
- [ ] 個人アカウントに依存していない（組織アカウントまたは共有アカウントで管理） — [§16-4](#16-4-データ管理バックアップ共有)

### 5. テスト・コード品質

- [ ] テストが全て通る（`pytest`） — [§8-2](#8-2-テストの実践)
- [ ] リンター・フォーマッターがエラーなし（`ruff check` / `ruff format --check`） — [§8-3](#8-3-コード品質ツール)
- [ ] CI/CDワークフロー（GitHub Actions）が設定されている — [§8-4](#8-4-cicd)

### 6. セキュリティ・倫理

- [ ] リポジトリにAPIキー・パスワード等の秘密情報がコミットされていない — [§20-1](#20-1-セキュリティの基礎)
- [ ] 依存パッケージに既知の脆弱性がない（`pip-audit`） — [§20-1-3](#20-1-3-依存パッケージのセキュリティ)
- [ ] 制限付きデータベースのDUA（データ利用規約）を遵守している（該当する場合） — [§20-2-1](#20-2-1-ヒトデータの法規制と利用規約)
