# コード例・コマンド例の説明不足チェック結果

- 作成日: 2026-03-20
- 目的: 全章のコードブロックを対象に、初心者（実験系生命科学者）が理解できるだけの説明があるかを監査

---

## 結果サマリ

| 章 | 問題数 | 深刻度 |
|----|--------|--------|
| §0 AIエージェント | 0 | — |
| §1 設計原則 | 0 | — |
| §2 ターミナル | 0 | — |
| §3 計算機科学の基礎 | 0 | — |
| §4 データフォーマット | 0 | — |
| §5 開発環境 | 3 | 中 |
| §6 Git | 3 | 中 |
| §7 テスト・品質管理 | 5 | 中 |
| §8 成果物 | 3 | 中 |
| §9 CLIツール | 0 | — |
| §10 データ処理 | 3 | 低〜中 |
| §11 可視化 | 2 | 低 |
| §12 ワークフロー | 1 | 低 |
| §13 コンテナ | 3 | 中 |
| §13A 実験管理 | 1 | 低 |
| §21 ソフトウェアの構成要素 | 4 | 低〜中 |

合計: **28箇所**（§0-§4は問題なし、§5以降に集中）

---

## 詳細リスト

### §5 開発環境（3件）

1. **行124付近 — `uv lock`コマンド**
   - `uv lock`が何を生成するのか、ロックファイルの役割が説明不足

2. **行313付近 — biocondaコラムのconda createコマンド**
   - `-c bioconda -c conda-forge`フラグの意味（チャネルの指定）、チャネル順序の意味が未説明

3. **行343付近 — MLコラムのPyTorch環境設定**
   - `pytorch-cuda=12.1`の意味、`-c pytorch -c nvidia`チャネルの必要性が未説明

### §6 Git（3件）

4. **行268付近 — `conda-lock -f environment.yml -p linux-64`**
   - `-p linux-64`の意味（プラットフォーム指定）が未説明

5. **行397付近 — Git LFSセットアップ**
   - `git lfs install`が何をするか、`.gitattributes`の役割と共有の必要性が未説明

6. **行432付近 — `sha256sum`チェックサムの生成・検証**
   - コマンドの出力形式、`-c`フラグでの検証時の表示が未説明

### §7 テスト・品質管理（5件）

7. **行257付近 — `pytest --cov=scripts --cov-report=term-missing`**
   - `--cov=scripts`が何を指定しているか、出力形式の説明が不足

8. **行329付近 — ruff設定の`[tool.ruff]`**
   - `target-version = "py310"`の意味、`line-length = 88`の根拠が未説明

9. **行348付近 — ruffのルール選択`select = [E, W, F, ...]`**
   - 各ルールコード（E, W, F, I, N, D, UP）の意味が未説明

10. **行453付近 — `.pre-commit-config.yaml`**
    - `rev: v0.8.6`の意図（バージョン固定）、`additional_dependencies`の役割が未説明

11. **行578付近 — GitHub Actions内のpytest-cov**
    - CI環境でのカバレッジ出力先の説明がない

### §8 成果物（3件）

12. **行92付近 — `pip install .` vs `pip install -e .`**
    - 通常インストールと開発モード（editable install）の違いが未説明

13. **行104付近 — PyPI公開コマンド（build, twine）**
    - `build`と`twine`の役割、sdist/wheelの用語、認証の必要性が未説明

14. **行573付近 — GitHub ActionsのYAML `run: |`**
    - `|`（複数行文字列リテラル）の意味が初心者に不明

### §10 データ処理（3件）

15. **行38付近 — `gc_content_vectorized()`のnp.frombuffer**
    - バイト配列への変換理由、`ord("G")`の役割が未説明

16. **行64付近 — `normalize_cpm()`の`np.where(col_sums == 0, 1, col_sums)`**
    - ゼロ除算回避の意図が未説明

17. **行283付近 — Bio.SeqIO, pysam等の1行コード例**
    - 各ライブラリの入出力が未説明（コラム内のため簡潔でよいが）

### §11 可視化（2件）

18. **行91付近 — Volcano plotの`np.select()`**
    - 3値分類のロジック（条件リストとvalueリストの対応）が未説明

19. **行220付近 — バイオリンプロットのtidy format**
    - seabornがtidy formatを要求する理由が未説明

### §12 ワークフロー（1件）

20. **行106付近 — Snakemake fastqcルールの`{sample}` wildcard**
    - wildcardの値がどう推定されるかの詳細が不足（直後に説明があるが、コード例の位置が先に来ている）

### §13 コンテナ（3件）

21. **行180付近 — ダイジェスト指定`@sha256:abc123...`**
    - ダイジェストとタグの違い、改変不可性の説明が不足

22. **行203付近 — レイヤーキャッシュの順序**
    - キャッシュがなぜ高速化するのかの仕組みの説明が不足

23. **行287付近 — docker-compose.ymlの`:ro`**
    - read-onlyマウントの意味が初出時に未説明（後述の本文で説明はある）

### §13A 実験管理（1件）

24. **行202付近 — JSONL形式の例**
    - JONLの利点（行追記の容易性）の説明が直後にあるが、コードブロックの前に置くべき

### §21 ソフトウェアの構成要素（4件）

25. **行65付近 — `ldd`出力の`=>`の意味**
    - シンボリックリンク解決の表記が未説明

26. **行86付近 — `shutil.which()`の戻り値**
    - PATH検索で得られるパスが何かが未説明

27. **行164付近 — `sys.path`リストの検索順序**
    - なぜこの順序なのかの理由が未説明

28. **行225付近 — 相対インポートの`..`**
    - ドット1つ/2つの階層カウント方法が未説明
